# Haladási napló

> Ez a fájl a `crm_projekt.md` korábbi "6. Aktuális állapot / Haladási napló" szekciója, kiszervezve 2026-07-26-án (kredit-takarékosság miatt — ez volt a leggyorsabban növő rész, minden szerkesztése az egész fájlt újra-lintelte). Szabály változatlan: minden munkamenet végén ide kerül egy új bejegyzés dátummal, a régieket nem töröljük, csak bővítjük. A fő `crm_projekt.md`-ben egy rövid pointer maradt.


> *(Minden munkamenet végén ide kerül egy új bejegyzés dátummal — a régieket nem töröljük, csak bővítjük.)*

- **2026-07-25:** Projekt elindult. Technológiai döntések meghozva (PHP+Laravel, MySQL, cweb hosting). Adatmodell első vázlata kész. Következő lépés: a pipeline-ok részletes kidolgozása szolgáltatásonként, és a coach-kereső oldal ↔ CRM viszonyának tisztázása.
- **2026-07-25 (2. munkamenet — Claude Code, `d:\AI\CRM` gépen):** Nekiálltunk a 2. fázisnak (Laravel projekt inicializálása Laragon alatt), de a gépen **nem volt telepítve Laragon** (sem XAMPP/WAMP, sem Composer, sem MySQL). Talált egy önálló, PATH-on kívüli **PHP 8.5.0**-t a `D:\php` alatt — ez nincs használatban, figyelmen kívül hagyható. Rob a wingetes automatikus telepítést választotta. A `winget` telepítve volt, de a `winget.azureedge.net` CDN nem volt elérhető erről a gépről/hálózatról (hálózati hiba, `laragon.org` és a GitHub-hostok viszont elérhetők voltak) — ezért a Laragon 8.6.1 teljes WAMP telepítőjét (`laragon-wamp.exe`, ~238 MB) közvetlenül letöltöttük GitHubról: `https://github.com/leokhoa/laragon/releases/download/8.6.1/laragon-wamp.exe` (mindig az `https://github.com/leokhoa/laragon/releases/latest` a friss verzió forrása). **Telepítés előtt kiderült, hogy mindkét meghajtón kritikusan kevés a szabad hely** ezen a gépen: `C:` 5,3 GB szabad (90 GB foglalt), `D:` 5,9 GB szabad (121,9 GB foglalt) — ez nem elég biztonságosan egy Laravel projekthez (vendor/, node_modules/, MySQL-adatok idővel több GB-ot is felemészthetnek). **Döntés:** Rob átvált egy másik notebookra, ahol van elég szabad hely, és ott folytatja a Laragon telepítést és a Laravel projekt inicializálását. Ezen a gépen (`d:\AI\CRM`) semmilyen telepítés/módosítás nem történt a rendszerben, csak a telepítő lett letöltve egy ideiglenes (session-scratchpad) mappába, ami nem került be a projektbe.
  **Következő lépés az új gépen:** (1) Laragon telepítése — előbb próbálkozz wingettel (`winget install --id LeNgocKhoa.Laragon -e --source winget`), ha a CDN ott sem elérhető, közvetlen letöltés a fenti GitHub-linkről és csendes telepítés (`laragon-wamp.exe /VERYSILENT /SUPPRESSMSGBOXES /NORESTART`) — **mindig a D: meghajtóra telepítve** (lásd 10. szekció); (2) ellenőrizni, hogy a Laragon PATH-ba teszi-e a PHP-t, Composert, MySQL-t; (3) Laravel projekt létrehozása a Laragon `www` mappájában (`composer create-project laravel/laravel crm` vagy `laravel new crm`); (4) MySQL adatbázis létrehozása + `.env` beállítása; (5) Git inicializálás + első commit (lásd Git-stratégia a 3. szekcióban — Claude Code automatikusan commitol); (6) ez a napló frissítése.
- **2026-07-25 (3. munkamenet):** Rob rögzítette az együttműködési elvárásait — lásd új **10. szekció**: mindig magyarul válaszoljunk, az AI dolgozzon önállóan (nem kritikus döntéseket automatikusan elfogadottnak tekintve, nem vár jóváhagyásra), jobb megoldási javaslat esetén beszéljük meg, minden telepítés a D: meghajtóra kerüljön, a Laragon telepítése elhalasztva (Rob később/munka közben telepíti), addig azokkal a feladatokkal haladunk, amikhez nem kell a telepített környezet, és az AI önállóan, kérés nélkül adjon státuszfrissítést nagyobb feladatok előtt/után. Következő lépés: a Laragon/környezet-függő munka szüneteltetve marad, helyette a 7. szekció (Nyitott kérdések) egyikén — a pipeline-lépések szolgáltatásonkénti kidolgozásán — érdemes továbbhaladni.
- **2026-07-25 (4. munkamenet — önálló munka, Laravel/Laragon nélkül):** Rob megerősítette: elmegy, egy másik notebookról folytatja majd (jelezni fogja), addig az AI **megállás nélkül, jóváhagyás-kérés nélkül** dolgozzon tovább mindazon, amihez nem kell telepített Laravel/Laragon. Külön kiemelte: a CRM-nek **kódolás nélkül** kell testreszabhatónak lennie bármilyen szakmára — ez nem új elv (lásd 3. szekció "Univerzalitás"), de a hangsúly erősödött, ezért a 3. szekció ki lett egészítve ezzel a konkrét elvárással.
  **Elkészült ebben a munkamenetben (Laravel-független tervezőmunka):**
  - [`docs/adatmodell.md`](docs/adatmodell.md) — a 4. szekció oszlop-szintű kifejtése, minden tábla mezőivel, azzal a mechanizmussal, ahogy a `service_types` + `pipelines`/`pipeline_stages` + `custom_field_definitions` hármas kódolás nélkül biztosítja az univerzalitást.
  - [`docs/schema.sql`](docs/schema.sql) — nyers MySQL DDL az egész sémára, hogy amint kész a Laravel-környezet, gyorsan Artisan-migrációkká alakítható legyen.
  - [`docs/architektura.md`](docs/architektura.md) — moduláris monolit felépítés (Contacts/Pipelines/Projects/CustomFields/Integrations/Automation modulok, funkció szerint, NEM szakma szerint), tenant-elkülönítés terve, belső REST API réteg terve, esemény-alapú hook-rendszer (Events/Listeners) konkrét példákkal (pl. `DealStageChanged` → won stage → automatikus `projects` létrehozás).
  - [`docs/pipeline-sablonok.md`](docs/pipeline-sablonok.md) — piszkozat pipeline-lépések mind az 5 induló szolgáltatáshoz (coaching, szervezetfejlesztés, webdesign, marketing, SEO) + javasolt egyedi mezők mindegyikhez + egy konkrét példa arra, hogyan bővülne a rendszer egy hatodik, teljesen új szakmával (fotózás) kódolás nélkül. **Ez Rob validálására vár**, nem végleges.
  - A 7. szekció (Nyitott kérdések) frissítve: a pipeline-kérdés "piszkozat kész" állapotba került; a számlázás kérdésre javasolt egy alapértelmezett irányt (MVP-ben csak követés-státusz, tényleges számlagenerálás későbbi integrációs modul) — nem kritikus, felülbírálható döntésként, hogy ne blokkolja a haladást.
  - **Git verziókövetés elindítva:** a `d:\AI\CRM` mappa eddig nem volt Git-repó — most inicializálva lett (`git init`), `.gitignore` létrehozva (kizárva `.env`, `/vendor`, `/node_modules` stb. a jövőbeli Laravel-projekthez), és ez a munkamenet lett az első commit. Ezután minden lényegesebb változtatás után az AI automatikusan commitol (lásd 3. szekció Git-stratégia).
  **Következő lépés:** további nyitott kérdéseken/dokumentáción lehet haladni Laravel nélkül (pl. REST API végpont-lista részletes kidolgozása, admin-felület wireframe terve a service_types/pipelines/custom_fields szerkesztéséhez), amíg Rob nem jelzi, hogy másik gépen folytatja és telepíthető a Laragon.
  **Folytatás, még ugyanebben a munkamenetben, tovább dolgozva:**
  - [`docs/api-tervek.md`](docs/api-tervek.md) — az `architektura.md`-ben vázolt belső REST API modulonkénti végpont-listája (Contacts, Pipelines, Projects, CustomFields, Integrations, jövőbeli coach-kereső webhook/SSO, super admin), hogy amint kész a Laravel-váz, gyorsan route-okká/kontrollerekké alakítható legyen.
  - [`docs/gdpr-terv.md`](docs/gdpr-terv.md) — konkrét folyamatterv: hozzájárulás-rögzítés, adatexport tartalma és végpontja, kétlépcsős törlés (azonnali soft delete + **javasolt 30 napos** megőrzés utáni automatikus, ütemezett végleges anonimizálás), azzal a feltételezéssel, hogy az üzleti/pénzügyi rekordok (deals/projects) a személyes adat törlése után is megmaradnak számviteli okból — **ezt érdemes lesz könyvelővel/jogásszal megerősíttetni élesítés előtt, nem blokkolja az MVP-t**.
  - [`docs/admin-felulet-terv.md`](docs/admin-felulet-terv.md) — szöveges wireframe-terv arról, milyen "Beállítások" felületen (Szolgáltatás-típusok / Pipeline-ok / Egyedi mezők szerkesztő) tudja majd Rob ténylegesen, kattintgatva, kódolás nélkül létrehozni egy vadonatúj szakmai profilt — ez a konkrét válasz a "ne kelljen fejlesztés" elvárásra. MVP-ben egyszerűsítésként javasolva egy Artisan konzol-parancs vagy közvetlen adatbázis-szerkesztés (phpMyAdmin, ami már elérhető `D:\phpMyAdmin-5.1.1-all-languages` alatt) is elég lehet, amíg a teljes admin-UI el nem készül.
  Mind a négy új `docs/` fájl és a `crm_projekt.md` kapcsolódó bővítései (3., 4. szekció linkek) egy második commitban kerülnek be a git-történetbe.
  **Következő lépés:** ha Rob visszatér és jelzi a gépváltást, először a környezet-ellenőrzés (10. szekció) jön, utána a `pipeline-sablonok.md` és a nyitott kérdések közös átbeszélése, majd a tényleges Laravel-projekt inicializálása a most lefektetett `schema.sql`/`api-tervek.md` alapján.
  **Még ugyanebben a munkamenetben, harmadik körben:** elkészült [`docs/teszt-personak.md`](docs/teszt-personak.md) — a 3. szekció "Teszt-personák" elvéhez 3 kidolgozott profil: Rob mint coach (valós, elsődleges), Kovács Anna mint kitalált webdesigner (második, párhuzamos szakma-teszt), és Szabó Márk mint kitalált asztalos/bútorkészítő (szándékosan más jellegű, fizikai-gyártási folyamat — ez teszteli legerősebben, hogy az architektúra valóban nem csak tanácsadói/digitális szakmákra univerzális). Ezzel lezárult ez a Laravel-független önálló munkaszakasz; összesen 8 új `docs/` fájl készült (`adatmodell.md`, `schema.sql`, `architektura.md`, `pipeline-sablonok.md`, `api-tervek.md`, `gdpr-terv.md`, `admin-felulet-terv.md`, `teszt-personak.md`), a `crm_projekt.md` több szekciója frissült, és a git-történet 3 commitra bővült.
  **Negyedik kör (Rob kérésére: a nyitott kérdések maradjanak a következő közös körre, addig önállóan tovább):** elkészült [`docs/csv-import-terv.md`](docs/csv-import-terv.md) (mezőtérképezés, duplikátum-kezelés, `maatwebsite/excel` csomag javaslat), [`docs/ertesitesek-terv.md`](docs/ertesitesek-terv.md) (Laravel beépített Notification-rendszerére épülő terv, egyedi tábla nem kell, esemény-alapú triggerek táblázata), [`docs/coach-kereso-integracio.md`](docs/coach-kereso-integracio.md) (konkrét webhook JSON payload + HMAC-aláírás, 5 perces egyszer használatos SSO-token folyamat lépésről lépésre), és [`CHANGELOG.md`](CHANGELOG.md) (verziószámozás elindítva, `[Unreleased] — tervezési fázis (0.0.x)`, következő cél v0.1.0 = MVP). A `crm_projekt.md` érintett bullet-jei (CSV-import, Értesítési rendszer, Verziószámozás, coach-kereső döntési napló) linkelve. Ez lesz a 4. git-commit. **Ötödik kör:** elkészült [`docs/jogosultsagok-terv.md`](docs/jogosultsagok-terv.md) — részletes jogosultsági mátrix owner/admin/member szerepkörökre, funkciónként (kontaktok, dealek, beállítások, userkezelés, számlázás, audit napló), Laravel Policy-alapú megvalósítási javaslattal. Ez lesz az 5. git-commit.
  **Hatodik kör (Rob kérésére: még van munkamenet-kapacitás, tovább kellett haladni nélküle):** a Laravel-projekt majdani gyors elindítását előkészítő anyagok készültek el, hogy amint fut a Laragon, ne kelljen menet közben tervezni: [`docs/csomag-terv.md`](docs/csomag-terv.md) (Composer/npm-csomaglista indoklással — Sanctum, activitylog, maatwebsite/excel, Tailwind+Alpine.js, mit NEM viszünk be még), [`docs/mappastruktura-terv.md`](docs/mappastruktura-terv.md) (konkrét mappafa + végrehajtható `php artisan make:...` parancssor minden modellre/kontrollerre/eseményre, és a `BelongsToAccount` trait mint a tenant-elkülönítés technikai kulcsa), [`docs/seeder-terv.md`](docs/seeder-terv.md) (kezdő adatok: Rob valós accountja + az 5 pipeline-sablon + a teszt-personák demo-accountként, csak local/testing környezetben), [`docs/teszterv.md`](docs/teszterv.md) (Pest-tesztlista: account-elkülönítés, jogosultsági mátrix, egyedi mezők validálása, esemény-hook, GDPR-folyamat, mindhárom teszt-personával), és [`docs/deployment-terv.md`](docs/deployment-terv.md) (cweb shared hosting checklist: document root, `.env`, cron-alapú scheduler/backup, SSL — nem MVP-blokkoló, csak előre elkészítve). Ez lesz a 6. git-commit.
  **Következő lépés:** a nyitott kérdések (7. szekció) és a `pipeline-sablonok.md` validálása Robbal közösen, amikor visszatér; a Laravel-független tervezőmunka ezzel gyakorlatilag lefedte mindazt, amit kódolás nélkül érdemes volt előkészíteni (adatmodell, architektúra, API, GDPR, admin-UI, teszt-personák, CSV-import, értesítések, coach-kereső integráció, jogosultságok, csomagok, mappastruktúra, seederek, tesztek, deployment) — a következő érdemi lépés már a tényleges Laravel-projekt inicializálása, ami Laragon-t igényel.
  **Hetedik kör:** elkészült [`docs/README.md`](docs/README.md) — a teljes `docs/` mappa tartalomjegyzéke és egy sorrendbe rendezett, végrehajtható checklist a Laravel-indítás napjára. Ez a 7. git-commit.
  **Nyolcadik kör (Rob kérésére: még tovább, amíg Laragon nélkül lehet):** elkészült [`docs/ui-wireframe-terv.md`](docs/ui-wireframe-terv.md) — a fő alkalmazás-képernyők (dashboard, kontaktok lista+részletek, kanban pipeline nézet drag-and-drop-pal, projektek, feladatok) szöveges wireframe-terve, konkrét reszponzív/mobil-viselkedési szabályokkal (táblázat → kártyás nézet mobilon), ez egészíti ki az `admin-felulet-terv.md`-t, ami csak a Beállítások képernyőket fedte le. Emellett [`docs/onboarding-terv.md`](docs/onboarding-terv.md) — "első lépések" folyamat új accountoknak, jelölve, hogy ez csak az 5-6. fázisban válik szükségessé, MVP-ben nem blokkoló. A `docs/README.md` checklist is frissítve a két új fájllal (10. és a bővített 12. pont). Ez a 8. git-commit.
  **Kilencedik kör (Rob kérésére: bőven van még idő/kredit, tovább):** elvégeztem egy **konzisztencia-átnézést** a 19 addigi `docs/` fájl között (a `schema.sql` és `adatmodell.md` tábla-listája, illetve a `mappastruktura-terv.md` modell-listája egyezik — nem találtam eltérést). Ezután 2 új terv készült: [`docs/biztonsag-terv.md`](docs/biztonsag-terv.md) (bemenet-validáció/XSS/SQL-injection, rate limiting, tenant-elkülönítés mint legkritikusabb pont, titkok kezelése, HTTPS, 2FA későbbre) és [`docs/riportok-terv.md`](docs/riportok-terv.md) (pipeline-tölcsér, win rate, bevétel szolgáltatásonként, Chart.js javaslat — ez egy AI-kezdeményezésű ötlet, nem eredeti elve a projektnek, Rob eldöntheti, hogy MVP-be kerüljön-e). **Az átnézés közben egy valódi, eddig nem dokumentált architekturális rés is előkerült**, amit a 7. szekcióba új nyitott kérdésként vettem fel (nem döntöttem helyette): a `projects` tábla egyszeri megbízást feltételez, de a marketing/SEO pipeline-sablonokban van "folyamatos/havi" lépés — ez egy ismétlődő (retainer) munka fogalmát igényelné, ami jelenleg nincs külön kezelve, és kihat a számlázásra is. Ez a 9. git-commit.
  **Tizedik kör:** két utolsó, kisebb terv készült el, mindkettő már meglévő, de eddig ki nem fejtett adatmodell-elemekre épül: [`docs/teljesitmeny-terv.md`](docs/teljesitmeny-terv.md) (cache/session/queue-driver választás a cweb shared hosting Redis-hiányához igazítva, N+1-elkerülés és lapozás mint kódolási alapszabály) és [`docs/lokalizacio-terv.md`](docs/lokalizacio-terv.md) (mit jelent a sémában már létező `locale` mező a gyakorlatban, mikor válik ténylegesen szükségessé a többnyelvűség). Ez a 10. git-commit.
  **Következő lépés:** ezzel a Laravel-független tervezőmunka mélysége meghaladja azt, amit ésszerű volt kódolás nélkül előre kitalálni — a fennmaradó nyitott pontok (pipeline-validáció, retainer-kérdés, ajánlatkészítő/szerződéskészítő modul állapota) mind Rob döntésére/tudására várnak, a többi pedig már tényleges Laravel-kódolást igényel.
- **2026-07-25 (5. munkamenet):** Tisztázódott a gépváltás logisztikája: (1) a projektmappa átviteléhez Rob GitHub-fiókot fog létrehozni és a már kész git-repót (10 commit) oda pusholjuk, alternatívaként egyszerű fájlmásolás is szóba jött; (2) a helyi AI-memória gépspecifikus, nem megy át — de emiatt szándékosan minden lényeges döntés/szabály bele lett írva ebbe a fájlba, úgyhogy ha az új gépen a Claude Code elolvassa ezt a fájlt, gyakorlatilag ugyanoda jut vissza. Rob jelezte: a következő gépen a projektmappa **C: meghajtóra** kerül (nem D:-re) — ezt ő maga készíti elő. **Pontosítás a 10. szekcióban:** a D:-re telepítés szabálya az első gép konkrét helyhiánya miatt született, nem általános elv — ezért a szöveg frissült, hogy jövőre gépenként újra kell ellenőrizni a szabad helyet, nem szabad vakon D:-t erőltetni.
- **2026-07-25 (6. munkamenet — Claude Code, `c:\AI\CRM` gépen, a gépváltás utáni első folytatás):** Ez az új gép, amit az 5. munkamenet említett (projektmappa a C: meghajtón, `c:\AI\CRM`). Környezet-ellenőrzés: a git-történet (10 commit) és mind a 24 `docs/` fájl épségben megvan, a C: meghajtón 128 GB szabad hely van (nincs helyhiány, mint az első gépen), Git telepítve volt, de PHP/Composer/MySQL/Laragon nem. Rob jóváhagyta, hogy ezen a gépen a Laragon a C: meghajtóra települjön.
  **Elvégzett munka:**
  1. **Laragon telepítve** wingettel (`winget install --id LeNgocKhoa.Laragon`) → `C:\laragon` alá, PHP 8.3.30 + Composer 2.9.4 + MySQL 8.4.3 + Apache/nginx/Redis/Mailpit stb. csomagban. A PHP-hez tartozó `php.ini` alapból nem létezett — létrehozva `php.ini-development`-ből, és bekapcsolva a Laravel-hez szükséges kiterjesztések (`openssl`, `pdo_mysql`, `mbstring`, `curl`, `fileinfo`, `intl`, `gd`, `mysqli`, `zip`). A PHP/Composer/MySQL bin-mappák bekerültek a felhasználói PATH-ba, hogy minden jövőbeli terminálban elérhetők legyenek parancssorból. A Laragon MySQL-je root/jelszó nélkül fut a `3306`-os porton (`C:\laragon\data\mysql-8.4` adatkönyvtár).
  2. **Két nyitott kérdés lezárva Robbal** (lásd 7. szekció): (a) a marketing/SEO pipeline-ok "folyamatos/havi" lépéséhez Rob a **külön `retainers`/`retainer_invoices` tábla** megoldást választotta (nem egyszerű mezőt a `projects`-en); (b) a számlázás MVP-ben **csak követés-státusz** (`invoice_status` mező), nincs tényleges számlagenerálás. Mindkét döntés átvezetve: [`adatmodell.md`](docs/adatmodell.md) (új `retainers`/`retainer_invoices` táblák, `invoice_status` mező a `deals`/`projects`-en), [`schema.sql`](docs/schema.sql) (DDL), [`architektura.md`](docs/architektura.md) (5. pont — `RetainerCreated`/`RetainerInvoicePeriodDue` események, a "won" hook most projektet VAGY retainert hoz létre, pipeline-konfigurációtól függően), [`pipeline-sablonok.md`](docs/pipeline-sablonok.md) (marketing/SEO szakasz pontosítva), [`mappastruktura-terv.md`](docs/mappastruktura-terv.md) (Retainer/RetainerInvoice modellek hozzáadva a scaffolding-tervhez).
  3. **A tényleges Laravel-projekt inicializálva** a repó gyökerében (`composer create-project laravel/laravel`, Laravel v13.22.0, PHP 8.3): a generált fájlok (app/, config/, database/, routes/, stb.) beköltöztetve a meglévő `docs/`/`crm_projekt.md` mellé, a `.gitignore` összefésülve a Laravel saját ignore-listájával. `.env` beállítva magyar locale-ra (`APP_LOCALE=hu`) és MySQL-re (`crm` adatbázis létrehozva). Migráció lefuttatva, `php artisan serve` teszttel ellenőrizve (HTTP 200, a welcome oldal elérhető `http://127.0.0.1:8000`-on). Telepítve + migrálva: `laravel/sanctum` (API-hitelesítés) és `spatie/laravel-activitylog` (audit napló) — mindkettő a `csomag-terv.md` szerinti alapkövetelmény.
  4. **Fontos munkamódszer-visszajelzés Robtól, rögzítve a memóriában is:** ne kérjek jóváhagyást apró/rutin lépésekhez, csak valódi kritikus döntéseknél — ez összhangban van a 10. szekció "Önálló munkavégzés" elvével, csak most Rob nyomatékosan megismételte. (Közben kiderült, hogy a munkamenet "Auto Mode" is aktiválva lett, ami technikailag is csökkenti a jóváhagyás-kéréseket.)
  **Még nyitott, Rob válaszára vár:** az ajánlatkészítő/szerződéskészítő modulok jelenlegi állapota (7. szekció) — ezt még nem sikerült tisztázni ebben a munkamenetben. A `pipeline-sablonok.md` teljes validálása (helyes-e minden lépés Rob tényleges munkafolyamatához) szintén még nyitott.
  **Következő lépés:** Rob döntésétől függően vagy azonnal folytatjuk a `schema.sql`/`mappastruktura-terv.md` alapján a tényleges modellek/migrációk/kontrollerek generálásával (3. fázis, MVP), vagy előbb megvárjuk a fenti nyitott pontok tisztázását. A `CRM.rar` (a régi gépről hozott átviteli archívum, 320 KB, untracked) egyelőre érintetlenül a mappában maradt — érdemes lesz törölni, ha Rob megerősíti, hogy már nincs rá szükség.

  **Folytatás, még ugyanebben a munkamenetben:** Rob válaszolt a két nyitott pontra — (1) az ajánlatkészítő/szerződéskészítő eszköze **már létezik, működik (nem 100%-os), web/HTML-alapú** (a mélyebb integráció mechanizmusa még nyitott, lásd 7. szekció); (2) belevágtunk a tényleges modellek/migrációk generálásába, **azzal a kikötéssel, hogy minden kódolás nélkül bővíthető/módosítható maradjon a CRM-en belül** — ez nem új elv, hanem a 3. szekció "Univerzalitás" pontjának nyomatékosítása, ugyanúgy, mint a 4. munkamenetben.
  **Elkészült:** mind a 17 tábla migrációja megírva a `schema.sql` alapján (`accounts`, `users` kiegészítve account_id/role/is_super_admin/locale mezőkkel, `service_types`, `pipelines`, `pipeline_stages`, `organizations`, `contacts`, `deals`, `projects`, `retainers`, `retainer_invoices`, `tasks`, `notes`, `documents`, `custom_field_definitions`, `subscriptions`, `integrations`, `api_keys`). Létrejött az `app/Models/Concerns/BelongsToAccount.php` trait + `app/Models/Scopes/AccountScope.php` — ez valósítja meg a tenant-elkülönítést: automatikus `account_id`-szűrés minden lekérdezésen, automatikus kitöltés létrehozáskor, és a `is_super_admin` felhasználóknak kivétel (ő mindent lát). Mind a 18 Eloquent modell elkészült a megfelelő kapcsolatokkal (Account, User, ServiceType, Pipeline, PipelineStage, Organization, Contact, Deal, Project, Retainer, RetainerInvoice, Task, Note, Document, CustomFieldDefinition, Subscription, Integration, ApiKey). **`php artisan migrate:fresh` lefutott hibátlanul**, és tinker-rel ténylegesen leteszteltem a legkritikusabb biztonsági szabályt: két különböző account-ot létrehozva, a második account usereként bejelentkezve az első account `service_types` rekordjai **nem látszottak** (`count() = 0`), és az új rekord automatikusan a helyes `account_id`-t kapta — a tenant-elkülönítés ténylegesen működik, nem csak terv szinten. A teszt-adatok utána törölve (`migrate:fresh` újra), tiszta adatbázis maradt.
  **Még hátra van (nem ebben a munkamenetben):** kontrollerek/API-route-ok (`api-tervek.md` alapján), a `DealStageChanged`→`Project`/`Retainer` esemény-hook tényleges megírása, policy-k (jogosultsagok-terv.md), seederek (Rob valós account+adatai, teszt-personák), és a Blade-felület. Ez a 3. fázis (MVP) további, nagyobb munkamenetet igénylő része.

  **Harmadik forduló, még ugyanebben a munkamenetben (Rob visszajelzései alapján):**
  1. **Rob be tudott lépni GitHub-ba** — a verziókövetés felhő-mentése (lásd 3. szekció Git-stratégia) ezzel elindulhat. Az AI-nak nincs GitHub API-hozzáférése/tokenje, ezért Robnak kell egy **üres** (README/`.gitignore` NÉLKÜLI) repót létrehoznia github.com-on, és megadnia a repó URL-jét — utána az AI hozzáadja remote-ként és pusholja a jelenlegi 12 commitot. A gépen a Git Credential Manager már be van állítva (`credential.helper=manager`), tehát push-kor egy böngészős GitHub-bejelentkezés/jóváhagyás fog felugrani Robnak.
  2. **Új, tartós tervezési elv rögzítve (akadálymentesség):** Rob és a leendő felhasználók köre vegyes látású — a felület sosem épülhet apró, fix betűméretre. Hozzáadva a 3. szekcióhoz + kifejtve [`docs/ui-wireframe-terv.md`](docs/ui-wireframe-terv.md) új 8. szekciójában (relatív mértékegységek, böngésző-nagyítás soha nem tiltható, min. 16px alapszöveg, kontraszt).
  3. **Retainer-koncepció pontosítva:** a havi díjas ismétlődő munka NEM csak marketing/SEO sajátossága — webdesignnál is van havi karbantartás (`pipeline-sablonok.md` webdesign 9. lépése most már `retainers`-re mutat, nem "opcionális, külön deal"). Rob kiemelte, hogy a retainerekhez **teendő-lista** is kell — ez már ma is működik a `tasks` tábla polimorf `taskable` kapcsolatán át (`Retainer::tasks()`), ezt explicit dokumentáltuk `adatmodell.md`-ben, és felvetettünk egy jövőbeli finomítást (automatikus, ismétlődő "teendő-sablon" havi periódusonként) — ez MVP-ben még nem szükséges, csak ötlet-szinten rögzítve.
  4. **Ajánlatkészítő/szerződéskészítő integráció ütemezése eldőlt:** Rob kifejezetten **nem** akarja most összekötni — előbb legyen működő alap CRM, az integráció egy későbbi, bonyolultabb lépés. A 7. szekció ezzel véglegesen lezárva (nem csak "részben").
  5. **`CRM.rar` törölve** — kicsomagolva ellenőriztem, kizárólag egy korábbi (a mai retainer-frissítések előtti) állapotot tartalmazott a már meglévő git-történetből, semmi új/hiányzó nem volt benne, biztonságos volt törölni.
  **Következő lépés:** Rob megadja az új GitHub-repó URL-jét, az AI hozzáadja remote-ként és pusholja a történetet. Utána folytatható a 3. fázis (kontrollerek, event-hook, UI) — ide már beépítve az akadálymentességi szabály a Blade/Tailwind-munkánál.

  **Negyedik forduló:** Rob megadta a GitHub-repót (`https://github.com/RobyWebDev/CRM`) — a git-történet pusholva (lásd lentebb a push eredményét). Emellett konkrét színvilág-kérés érkezett: alapértelmezett téma **sötét, de nem fekete**, referenciaként a cib.hu sötétzöldjével. Ahelyett, hogy találgattam volna a pontos árnyalatot, **ténylegesen lekértem a cib.hu élő CSS-ét** (`curl`), és megkerestem benne a ténylegesen használt hex-kódokat — a domináns szín `#0b4a35` (489 előfordulás). Erre építve elkészült [`docs/szinvilag-terv.md`](docs/szinvilag-terv.md): teljes sötét + világos téma paletta, minden kontrasztarány ténylegesen kiszámolva (WCAG relatív luminancia képlet), a legfrissebb WCAG 2.1/2.2 szabályokra hivatkozva (1.4.11 nem-szöveges kontraszt, 2.4.11 fókusz nem takarható, 2.5.8 cél mérete). Linkelve a 3. szekcióból és a `docs/README.md` checklistből.

  **Ötödik forduló — GitHub push akadály + színfinomítás:** A push megpróbálásakor kiderült, hogy a gépen egy **más GitHub-fiók (`IceBoby`) hitelesítő adata** volt gyorsítótárazva, és ezzel próbált pusholni a Git — 403 hibával, mert `IceBoby`-nak nincs jogosultsága a `RobyWebDev/CRM` repóhoz. Az ellenőrzés közben véletlenül **valódi, élő GitHub-token villant fel** a terminál-kimenetben — ezt azonnal jeleztem Robnak biztonsági okból, és **nem töröltem/módosítottam semmilyen hitelesítő adatot**, amíg nem tisztázódik, kié ez a fiók. A GitHub publikus API-jával (`api.github.com/repos/RobyWebDev/CRM`) megerősítettem: a `RobyWebDev` és az `IceBoby` **ténylegesen két különböző GitHub-felhasználói fiók** (eltérő numerikus user ID), még ha Rob esetleg mindkettőt sajátjának is gondolja. Rob megerősítette, hogy szerinte mindkettő az övé, és kérte, hogy **semmit ne töröljünk**. **Megoldási javaslat, amit még nem hajtottunk végre** (Rob döntésére vár): mivel törlés nélkül kell megoldani, a legegyszerűbb út, ha Rob a `https://github.com/RobyWebDev/CRM/settings/access` oldalon collaboratorként hozzáadja az `IceBoby` fiókot a repóhoz — utána a meglévő (nem törölt) `IceBoby`-hitelesítéssel simán menni fog a push, mert a JOGOSULTSÁG rendeződik, nem a hitelesítő adat.
  Emellett Rob nyitott volt egy jobb színjavaslatra, és kérte, hogy a legfrissebb webdesign-elveket (fluid méretezés, `clamp()`) is építsük be már most, az elejétől. Ennek eredménye: a `docs/szinvilag-terv.md` 3. szekciója **OKLCH (CSS Color Module 4) alapú, perceptuálisan egyenletes palettává** lett átdolgozva — megtartva a cib.hu-zöld hue-t (~165°), de a sok órás munkahasználatra kényelmesebb, szisztematikusan felépített változatban (oklch() elsődleges deklaráció + hex fallback minden tokenre, mindegyik kontrasztaránya ténylegesen kiszámolva). Új dokumentum: [`docs/tipografia-layout-terv.md`](docs/tipografia-layout-terv.md) — fluid tipográfia/spacing-skála `clamp()`-pel, container query-k, `dvh`, logikai CSS-tulajdonságok, `:focus-visible`, `prefers-reduced-motion`/`prefers-contrast` — mindezt a Tailwind-alapú frontendbe illesztve, amikor majd tényleg elkezdődik a UI-munka.
  **Hatodik forduló — GitHub push sikeresen megoldva:** Rob pontosította, hogy nem collaborator-hozzáadást szeretne, hanem friss bejelentkezést/engedélyezést. A böngészős OAuth-bejelentkezés a **Greenfox szervezet OAuth-alkalmazás-korlátozásába ütközött** (Rob egy korábbi Greenfox-projektnél használta ugyanezt a gépet/böngészőt, és a szervezet szervezeti jóváhagyást követelt volna). Ennek megkerülésére Rob egy **Personal Access Token**-t generált (`repo` scope, 90 napos lejárat) — ezt biztonságosan eltároltam a Git hitelesítő-kezelőjében (`git credential approve`), és a push **sikeres volt**: mind a 16 commit felkerült a `https://github.com/RobyWebDev/CRM` repóba, ellenőrizve a GitHub API-val is. A verziókövetés felhő-mentése ezzel véglegesen üzemel — mostantól minden jövőbeli commit egyszerűen pusholható.
  **Következő lépés:** a 3. fázis (MVP) folytatása — API-kontrollerek, esemény-hook, Blade-felület, ez utóbbi már a most lefektetett `szinvilag-terv.md`/`tipografia-layout-terv.md` alapján.

  **Hetedik forduló — az első látható felület elkészült:** Rob rákérdezett, hogyan tovább — a döntés (lásd a beszélgetés) az volt, hogy a leggyorsabb út egy tényleges, böngészőben látható eredmény felé menjen, ne a háttér-réteg (API/policy-k) folytatása. Elkészült:
  1. **Laravel Breeze telepítve** (Blade stack, `laravel/breeze` + `npm install && npm run build`) — bejelentkezés, regisztráció, jelszó-visszaállítás, profilkezelés kész scaffoldingként.
  2. **Minden Breeze-nézet és -komponens átírva** a `szinvilag-terv.md`/`tipografia-layout-terv.md` szerinti saját tokenekre — a Tailwind alap szürke/fehér/indigó paletta helyett `bg-page`/`bg-surface`/`text-ink`/`bg-accent` stb. szemantikus osztályok (CSS egyéni tulajdonságokként `resources/css/app.css`-ben, oklch()+hex fallback-kel), és `text-fluid-*`/`p-fluid-*` fluid méretezés mindenhol. A `tailwind.config.js` ezekre a tokenekre mutató színeket/fontSize-t/spacing-et regisztrál.
  3. **Seederek** (`AccountSeeder`, `PipelineTemplatesSeeder`): Rob valós "CoachLab" accountja + usere (`robert.rado@gmail.com`, ideiglenes jelszóval — **javasolt lecserélni bejelentkezés után**), és mind az 5 szolgáltatás (coaching, szervezetfejlesztés, webdesign, marketing, SEO) pipeline-ja/lépései/egyedi mezői a `pipeline-sablonok.md` alapján — 51 pipeline-lépés, 15 egyedi mező.
  4. **Kontaktok modul** (`ContactController` + `resources/views/contacts/*`): lista (kereséssel), létrehozás, megtekintés, szerkesztés, törlés — az első ténylegesen használható CRM-funkció.
  5. **Végigtesztelve**: `migrate:fresh --seed`, bejelentkezés, kontakt létrehozása/megjelenítése böngésző-szimulált (curl) kéréseken keresztül; a tenant-elkülönítés és a magyar ékezetes karakterek UTF-8 tárolása is külön ellenőrizve (egy hamis 500-as hiba a tesztelés közben kizárólag a curl-parancsom karakterkódolási hibája volt, nem alkalmazáshiba — a böngészőből érkező valós beküldés ettől függetlenül helyesen működik, amit percent-encode-olt teszttel meg is erősítettem).
  6. A helyi szerver fut (`php artisan serve`, `http://127.0.0.1:8000`) — Rob ki tudja próbálni Chrome-ban.
  **Következő lépés:** Rob böngészőben kipróbálja (bejelentkezés a fenti e-maillel, jelszócsere javasolt), visszajelez a színekről/élményről; utána folytatható akár a pipeline/deal-kanban felület, akár a háttér-réteg (policy-k, esemény-hook, API).

  **Nyolcadik forduló — Pipeline/Kanban nézet:** Rob bejelentkezett, visszajelzése: **a design tetszik** (nem finomítunk rajta tovább egyelőre). Ezután eldöntöttük, hogy a Kontaktok után a **Pipeline/Kanban nézet** legyen a következő látható modul (a döntési kérdés opciói között volt még: pipeline-validáció, projektek/teendők, háttér-réteg — ezeket későbbre hagytuk).
  **Elkészült:** `DealController` (kanban lista pipeline-választóval, létrehozás, szerkesztés, "mozgatás" lépések között, törlés) + `resources/views/deals/*` — a board oszlopai a pipeline lépései, kártyák a dealek. **Fontos akadálymentességi döntés:** a kártyákon a lépés-váltás egy `<select>`-tel történik (nem kizárólag drag-and-drop), ez a **WCAG 2.2 új 2.5.7 "Dragging Movements" szabálya** miatt kötelező — a húzás-alapú interakciónak legyen egyetlen-mutatóeszközös (klikk/érintés) alternatívája is. Tényleges drag-and-drop (mint vizuális kiegészítés a select mellé) egy jövőbeli finomítás lehet, nem MVP-blokkoló.
  A `pipeline_stage_id` váltásnál a kontroller automatikusan `won`/`lost` állapotba állítja a dealt, ha a cél-lépés `is_won_stage`/`is_lost_stage` — ez funkcionálisan megegyezik az `architektura.md`-ben tervezett `DealStageChanged` esemény-hook viselkedésével, de egyelőre **közvetlenül a kontrollerben**, nem külön Laravel Event/Listener formájában (MVP-egyszerűsítés — a tényleges esemény-alapú szétválasztás akkor éri meg, amikor több helyről is ki kell váltani ugyanazt a logikát, pl. API-ról is).
  **Végigtesztelve** (curl): deal létrehozása, "won" lépésre mozgatása → `status=won` + `closed_at` automatikusan kitöltve; egy kezdeti hibát is találtam és javítottam menet közben — a kanban eredetileg csak a `status=open` dealeket mutatta minden oszlopban, ami azt jelentette, hogy a "Lezárva" oszlopokba mozgatott dealek eltűntek a tábláról; ezt javítottam, hogy minden dealt mutasson a jelenlegi lépésén, státusztól függetlenül.
  **Következő lépés:** további modulok (projektek/teendők a "won" deal után, vagy a háttér-réteg) Rob következő döntésére várnak.

  **Kilencedik forduló — nagy, több részes kérés (Leadek, több paletta, drag-and-drop, CRM best practice-ek):** Rob egy nagy, több irányba mutató kérést fogalmazott meg: (1) Lead-kezelés kelljen; (2) AI-alapú kutatási lehetőség; (3) egyéb CRM best practice-ek beépítése; (4) NE egyetlen rögzített színpaletta legyen, hanem több választható, kész, WCAG-nak megfelelő, mindegyik sötét+világos módban — a Salesforce világoskék/fehér stílusát konkrétan javasolta induló második palettaként; (5) ablakok/panelek legyenek átméretezhetők, és fogadjanak el beillesztett infót; (6) pontosítás, hogy a modern/friss technológiákat NEM kell kerülni mozgássérültek miatt, mert nem várható jelentős arányban ilyen felhasználó — a kontraszt/olvashatóság viszont továbbra is szigorúan betartandó (ez utóbbi Robot magát is érinti).
  **Elkészült ebben a körben:**
  1. **Több-paletta témarendszer, ténylegesen megvalósítva** (nem csak terv): a "Forest" (meglévő zöld) mellé egy **"Salesforce" paletta**, aminek színeit — a cib.hu-s módszerrel megegyezően — **ténylegesen lekértem a Salesforce Lightning Design System élő CSS-éből** (`#0176d3` kék, navy sötét változat stb.), nem találgattam. Mindkét paletta sötét ÉS világos módban is elérhető (4 kombináció összesen), `data-palette`/`data-theme` HTML-attribútumokkal vezérelve, `accounts.theme_palette` (fiók-szintű) + `users.theme_mode` (személyes) mezőkkel tárolva, egy új "Megjelenés" szekcióval a Profil oldalon (`ThemeController`). **Menet közben egy valódi hibát is találtam és javítottam:** a Forest paletta világos módjában a sikeres/hiba/figyelmeztetés-színek eredetileg csak 1.5-2:1 kontrasztot adtak (WCAG-bukás) — ezeket világos módra tervezett, sötétebb változatokra cseréltem. Részletek: `docs/szinvilag-terv.md` (átdolgozva, 5-6. szekció).
  2. **Leads (lead-kezelés) modul** — CRM best practice (Salesforce Lead-objektum egyszerűsített megfelelője): külön `leads` tábla/modell/kontroller/nézetek, állapot (új/kapcsolatfelvétel/minősített/elutasított/konvertált), egyszerű 0-100 lead-pontszám, és egy **"Konvertálás kontaktá"** funkció, ami Contactot (és ha van megadva érdeklődési terület, Dealt is az adott szolgáltatás alapértelmezett pipeline-jának első lépésén) hoz létre. **Itt is találtam és javítottam egy hibát:** a `converted_contact_id`/`converted_deal_id`/`converted_at` mezők hiányoztak a modell tömeges-kitöltési (`Fillable`) listájából, ezért csendben nem mentődtek — ezt észrevettem tesztelés közben (a redirect helyes contact-ID-re mutatott, de a lead rekordon a mezők üresek maradtak), és kijavítottam.
  3. **Valódi drag-and-drop a Kanban táblán** (natív HTML5 DnD + Alpine.js), a korábbi egyszerű `<select>` MELLETT (nem helyette) — Rob pontosítására, hogy a modern interakciót nem kell kerülni, a `<select>` viszont WCAG 2.5.7 miatt megmarad kötelező alternatívaként.
  4. **Dokumentált, de tudatosan NEM megépített elemek** (mert vagy döntést, vagy külön munkamenetet igényelnek): az **AI kutatási funkció** — ez külső, nem ingyenes API-t igényelne, a Költség-elv szerint ezt előre meg kell beszélni Robbal (melyik szolgáltató, várható költség), plusz GDPR-átgondolást is igényel; **átméretezhető panelek és paste-barát mezők** — akkor konkrét, amikor a tényleges widget-gazdag képernyők megépülnek; **egyéb best practice-ötletek** (aktivitás-idővonal, duplikátum-felismerés, e-mail-integráció, lead-pontszám finomítása). Mindegyik rögzítve a 8. szekció (Ötlet-backlog) alatt.
  **Végigtesztelve:** paletta-váltás (curl-lel, a `data-palette` attribútum ténylegesen megváltozott a HTML-ben), lead létrehozása + konvertálása (kontakt+deal létrejötte ellenőrizve tinkerrel), drag-and-drop markup (draggable attribútum jelenléte).
  **Következő lépés:** Rob dönt, hogy az AI-kutatás API-választása/költsége miatt mikor térünk vissza rá; addig folytatható a háttér-réteg (policy-k, esemény-hook) vagy további látható modulok (projektek/teendők).

  **Tizedik forduló — a kanban vízszintes görgetés lecserélve:** Rob visszajelezte, hogy a kanban tábla oldalra görgetős elrendezése **nem praktikus, nem eléggé átlátható** — kérte, hogy vegyünk át jól bevált megoldásokat bármilyen CRM-ből (nem csak Salesforce), amik kis projektbe is illenek.
  **Elkészült:**
  1. **Új alapértelmezett nézet: rendezett lista/táblázat** (`deals.index`) a vízszintesen görgetős oszlopok helyett — egy dealt-listát mutat (asztali táblázat, mobilon kártyák, a meglévő reszponzív konvenció szerint), lépésenkénti darabszám-jelvényekkel (nem görgetős, tördelődő sorban), és a lépés-váltás ugyanazzal az akadálymentes `<select>`-tel megy soronként.
  2. **A régi kanban tábla megmaradt**, opcionális második nézetként (`?view=board`, "Tábla nézet" gomb) — nem veszett el a munka, csak nem ez az alapértelmezett.
  3. **Két új CRM best practice, kis projektbe is illő, más rendszerekből átvéve** (nem csak Salesforce): **"hány napja ebben a lépésben"** jelzés dealenként (HubSpot-stílusú "elakadt üzlet" figyelmeztetés 14+ nap felett — új `deals.stage_entered_at` mező, ami a tényleges lépésváltáskor frissül, NEM ugyanaz, mint az `updated_at`); **súlyozott (forecast) pipeline-érték** (nyitott dealek értéke × a lépéshez tartozó valószínűség — a `pipeline_stages.probability` mező már a séma része volt kezdettől, csak eddig nem használtuk; Pipedrive/Salesforce forecast-nézetének megfelelője). Emellett egy **"elvesztés oka"** mező (`deals.lost_reason`) is bekerült — CRM best practice, hogy tanulni lehessen a bukott üzletekből, csak akkor jelenik meg a szerkesztő űrlapon, ha a deal állapota "elveszett".
  **Végigtesztelve** (curl): lista nézet + tábla nézet mindkettő betöltődik, súlyozott érték helyesen számol (100.000 Ft × 50% alapértelmezett valószínűség = 50.000 Ft, mivel a seedelt pipeline-okon még nincs kitöltve konkrét `probability` érték), "0 nap" helyesen jelenik meg új dealnél, elvesztés-oka mező megjelenik és mentődik, ha a deal elveszettre vált.
  **Következő lépés:** Rob kipróbálja az új lista-nézetet; ha jó, a `pipeline_stages.probability` értékek tényleges kitöltése (jelenleg minden lépésen `NULL`, 50%-os alapértelmezéssel számol) egy jövőbeli finomítás lehet, ha pontosabb forecast kell.

  **Tizenegyedik forduló — fiók törlés jogosultsághoz kötve + automatikus adatmentés-felajánlás:** Rob kérése: a "Fiók törlése" funkció csak owner/admin szerepkörnek legyen elérhető (sima "member" usernek ne), és törléskor automatikusan ajánljon fel egy adatmentési (export) lehetőséget. A paletta-admin funkciók (új paletta létrehozása, meglévők másolása/módosítása, mindenki számára publikálás) és a mezőnevek admin általi átírása/angol nyelvi változat kifejezetten **későbbi lépésként** lett megjelölve — ezeket nem építettem meg, csak a 8. szekció (Ötlet-backlog) alá kerültek részletesen.
  **Elkészült:**
  1. **Szerepkör-alapú védelem**: a "Fiók törlése" szekció (Profil oldal) mostantól csak `owner`/`admin` szerepkörnél jelenik meg a Blade-nézetben, ÉS a `ProfileController::destroy()` szerver-oldalon is elutasítja (403) egy esetleges "member" próbálkozást — nem csak UI-szinten rejtett, ténylegesen védett.
  2. **Automatikus adatmentés-felajánlás**: új `GET /profile/export` végpont (`ProfileController::export`, ugyanúgy owner/admin-hoz kötve), ami egy letölthető JSON-fájlba menti a fiók legfontosabb adatait (szervezetek, kontaktok, leadek, dealek, projektek, retainerek). Ez a GDPR-terv.md-ben már korábban tervezett adatexport funkció első, működő verziója. A törlés-megerősítő ablak (modal) automatikusan megjeleníti az "Adatok mentése most" gombot, mielőtt a jelszóval megerősített törlésre kerülne sor.
  **Végigtesztelve**: export-végpont ténylegesen letölthető JSON-t ad vissza helyes fájlnévvel (`Content-Disposition: attachment`), a szerepkör-ellenőrzés logikája (`in_array($role, ['owner','admin'])`) tinkerrel megerősítve.
  **Következő lépés:** a 8. szekcióban rögzített két backlog-tétel (paletta-admin, mezőnév-testreszabás) Rob jelzésére vár, mikor kerüljenek sorra.

  **Tizenkettedik forduló — nyelv szerint helyes névsorrend, ténylegesen megvalósítva:** Rob kérése: magyar nyelvben magyar konvenció szerinti névsorrend (vezetéknév elöl), angolban (US) angol szabvány szerint (keresztnév elöl) — mindenhol, ahol személynév jelenik meg vagy szerkeszthető (Kontaktok, Leadek). Emellett megismételte az admin-szintű széles szabadság igényét ("mint egy profi CRM-ben a fejlesztőnek vagy az adminnak") — ez a korábban rögzített két backlog-tételt (paletta-admin, mezőnév-testreszabás) erősíti meg, nem hozott új konkrét, most megépítendő funkciót.
  **Elkészült:**
  1. **`App\Models\Concerns\HasPersonName`** trait — `full_name` accessor, ami a bejelentkezett user (vagy account) `locale` mezője alapján dönti el a sorrendet. `Contact` és `Lead` modell mindkettő használja.
  2. **`<x-name-fields>`** Blade-komponens — a kereszt-/vezetéknév mezőket nyelv szerint helyes sorrendben jeleníti meg (a vizuálisan első mezőn `autofocus`), négy helyen váltva ki a korábbi kézzel írt, fix sorrendű mezőpárokat: `contacts/create`, `contacts/edit`, `leads/create`, `leads/edit`.
  3. **Minden névmegjelenítés** (kontakt-lista, kontakt-részletek, lead-lista, lead-szerkesztés, deal-lista/kanban/űrlapok) a régi `trim($x->first_name.' '.$x->last_name)` minta helyett most `$x->full_name`-et használ — egyetlen helyen (`HasPersonName`) módosítható a teljes rendszer viselkedése.
  4. A mechanizmus **most is működik** (mivel minden `locale` jelenleg `hu`), és készen áll arra, hogy amint egy account/user `locale`-ja `en`-re vált, a sorrend automatikusan angol konvencióra váltson — nem igényel új kódot.
  **Végigtesztelve**: új kontakt létrehozva (`first_name=Anna, last_name=Kovacs`) → a listában helyesen "Kovacs Anna" jelenik meg (vezetéknév elöl); a form-mezők sorrendje (`for="last_name"` a HTML-ben `for="first_name"` előtt) is megerősítve.
  **Következő lépés:** a névsorrend-mechanizmus dokumentálva `lokalizacio-terv.md` új 2b-2c szekciójában, összekötve a jövőbeli admin-szintű mezőnév-testreszabás backlog-tétellel.

  **Tizenharmadik forduló — admin-szabadság best practice-lista + Projektek/Teendők modul:** Rob megkérdezte, milyen további admin-konfigurációs területeket érdemes átvenni nagy CRM-ekből — 7 tételt soroltam fel (automatizáció-szerkesztő, sablonok, riport-builder, finomabb jogosultságok, audit napló UI, import/duplikátum-szabályok, mező-sorrend szerkesztő), Rob kérésére mindet a 8. szekció (Ötlet-backlog) alá írtam, most nem építettük meg őket. Ezután a következő konkrét munka a **Projektek és teendők modul** lett (a korábbi irány-döntő kérdésre adott válasz).
  **Elkészült:**
  1. **`pipelines.won_creates`** mező (`project`/`retainer`/`none`) — ez dönti el, mit hozzon létre automatikusan a rendszer, amikor egy deal "won" lépésre kerül. A seederben a marketing és SEO pipeline `retainer`-re állítva (a korábbi retainer-döntésnek megfelelően), a többi `project` (alapértelmezés).
  2. **Automatikus Project/Retainer létrehozás** (`DealController::maybeCreateProjectOrRetainer`) — amint egy deal won-ba kerül (mozgatással vagy szerkesztéssel), a rendszer a pipeline beállítása alapján létrehozza a megfelelő rekordot (cím/kontakt/szervezet/érték átmásolva a dealből), és csak egyszer (véd az ismételt létrehozás ellen, ha egy deal ki-be mozogna a won lépésen).
  3. **Projektek modul** (`ProjectController` + `resources/views/projects/*`): lista (állapot szerint szűrhető), részletek (kontakt/deal/büdzsé/időszak + teendő- és jegyzet-lista), szerkesztés (állapot, számlázási státusz stb.).
  4. **Retainerek modul** (`RetainerController` + `resources/views/retainers/*`): ugyanaz a mintázat, plusz a számlázási periódusok (`retainer_invoices`) megjelenítése, ha vannak.
  5. **Újrahasznosítható `<x-task-list>` és `<x-note-list>` Blade-komponensek** — polimorf teendők/jegyzetek bármely entitáshoz (most Project és Retainer használja, de Contact/Deal is bekapcsolható később egy sorral). `TaskController` (felvétel, kész/nem kész váltás, törlés) és `NoteController` (felvétel) új, kis kontrollerek.
  6. Navigáció ("Projektek", "Retainerek" menüpont) és a Dashboard mind az 5 csempéje (leadek, kontaktok, dealek, projektek, retainerek) valós adatot mutat.
  **Végigtesztelve** (curl): egy coaching-dealt "won"-ra mozgatva automatikusan létrejött egy Project (helyes címmel/büdzsével); egy marketing-dealt "won"-ra mozgatva automatikusan létrejött egy Retainer (helyes havi díjjal); teendő felvétele/készre jelölése/törlése és jegyzet felvétele a projekt-részletek oldalon mind működik. A migráció additív volt, de mivel a `won_creates` mezőt utólag is be kellett tölteni a már létező (seedelt) pipeline-sorokba, egy `migrate:fresh --seed`-et futtattam — ez biztonságos volt, mert a fiókban nem volt valós adat, csak a korábbi tesztek maradványai (amiket úgyis töröltem).
  **Fontos, Robnak szóló mellékhatás:** a `migrate:fresh` a munkamenet-táblát is újraépítette, ezért a böngészőben addig bejelentkezve tartott munkamenete megszakadt — újra be kellett jelentkeznie (`robert.rado@gmail.com` / `changeme123`, ha még nem cserélte le).
  **Következő lépés:** Rob kipróbálja a Projektek/Retainerek modult egy valódi deal "won"-ra mozgatásával; utána eldönthető, hogy a fennmaradó nyitott kérdések (pipeline-validáció, ajánlatkészítő-integráció), vagy a 8. szekció backlog-tételei közül melyik legyen a következő fókusz.

  **Tizennegyedik forduló — nagy ötletlista (MiniCRM, onboarding, súgó, insights, biztonság, aktivitás-menedzsment) + valós biztonsági teszt:** Rob egy nagyon széles, sok-részes kérést fogalmazott meg, explicit kéréssel, hogy MOST csak a 8. szekció (Ötlet-backlog) alá kerüljön minden, eldöntendő legyen később — lásd ott a részletes, 10 pontos listát + 5 saját (AI-javasolt) kiegészítést. Kiemelte a biztonságot mint legfontosabbat.
  **Ténylegesen elvégzett munka (nem csak backlog):**
  1. **MiniCRM-kutatás** — a magyar MiniCRM valós dokumentációjából (nem találgatásból) azonosítottam két konkrét, átvehető mintát: kattintható "státuszfa" pipeline-áttekintő, és idő-alapú automatikus emlékeztető ("4 napja nincs továbblépve az ajánlat → automatikus teendő") — ez utóbbi közvetlenül ráépül a már meglévő `stage_entered_at` mezőnkre.
  2. **Élő biztonsági penetrációs teszt a tenant-elkülönítésre:** létrehoztam egy MÁSODIK, teljesen külön teszt-fiókot/usert, és mint az a user megpróbáltam megtekinteni/szerkeszteni/törölni Rob CoachLab-fiókjának egy kontaktját és dealjét közvetlen URL-lel. **Minden próbálkozás 404-et adott**, a támadó saját listái üresek maradtak, a megcélzott rekordok érintetlenek maradtak. Ez konkrétan megerősíti (nem csak állítja), hogy a `BelongsToAccount` + route-model-binding kombináció strukturálisan zárja ki a fiókok közötti hozzáférést, minden CRUD-műveletnél. Teszt-adatok utána törölve.
  **Következő lépés:** a nagy ötletlista Rob döntésére vár, melyik elem mikor kerüljön sorra — addig folytatható a korábbról nyitva maradt irány (pipeline-validáció, ajánlatkészítő-integráció, vagy bármely 8. szekció-beli tétel Rob választása szerint).

  **Tizenötödik forduló — mélyreható MiniCRM-kutatás + önálló, megállás nélküli megvalósítás:** Rob elment, és kifejezetten kérte, hogy (1) a MiniCRM-nek nézzek utána SOKKAL alaposabban (ne csak kisvállalkozói szűkítéssel), (2) addig önállóan, megállás nélkül, instrukciók nélkül dolgozzam, amíg csak lehet, (3) konkrétan a kontakt-felvitelnél legyen több hasznos mező + szabad jegyzet/megjegyzés lehetőség.
  **Elkészült:**
  1. **Alapos, valós dokumentációra épülő MiniCRM-kutatás** — új, önálló dokumentum: [`docs/minicrm-inspiracio.md`](docs/minicrm-inspiracio.md). Ténylegesen lekért/összegyűjtött (nem kitalált) információ: a teljes MiniCRM termékportfólió (Értékesítés, Számlázás, Projektek, HR, Ügyfélszolgálat, Eszközök stb. — megerősíti a mi moduláris-monolit elvünket), az adatlap-mezőtípusaik (beleértve egy nálunk még hiányzó **fájlmező** típust), az automatizáció-builder pontos felépítése (trigger+6 akciótípus, konkrét példákkal), riport-/BI-mutatóik, teendő/naptár-mintáik (ismétlődő teendő, Google Naptár-szinkron), címke-rendszerük, webes űrlap+Facebook lead-integrációjuk, telefónia-integrációjuk, jogosultsági mintájuk. A dokumentum végén világosan elkülönítve: mit érdemes MOST megépíteni vs. mi marad backlogban.
  2. **Kontakt-adatlap bővítve** (Rob konkrét kérése): új mezők — beosztás, születésnap, weboldal, cím. **Szabad szöveges jegyzet felvehető rögtön a kontakt létrehozásakor** (nem kell külön oldalra menni utána) — ez egy `Note` rekordot hoz létre automatikusan. A kontakt-részletek oldal mostantól teendő-listát ÉS jegyzet-listát is mutat (a már meglévő `<x-task-list>`/`<x-note-list>` komponensek újrafelhasználásával, amiket eddig csak Project/Retainer használt).
  3. **Címkék (tags) rendszer**, MiniCRM-mintára: új `tags`/`taggables` tábla, `HasTags` trait (Contact és Organization modellen), vesszővel elválasztott szöveges beviteli mező (a nem létező címke automatikusan létrejön az accounthoz), kattintható címke-pill-ek a kontakt listán/részleteken, és `?tag=` szűrés a kontakt-listán.
  4. **Ismétlődő teendők**, MiniCRM-mintára: `tasks.recurrence` mező (`daily`/`weekly`/`monthly`); amikor egy ismétlődő teendőt készre jelölnek, a rendszer automatikusan létrehozza a következő előfordulást, a határidőt megfelelően eltolva (`Task::nextDueDate()` + `TaskController::toggle()`).
  **Végigtesztelve** (curl, mindegyik önállóan, felügyelet nélkül): kontakt létrehozása minden új mezővel + címkékkel + jegyzettel egyben (mind helyesen mentve/megjelenítve); címke szerinti szűrés működik; egy havonta ismétlődő teendő készre jelölése után ellenőriztem, hogy a következő előfordulás ténylegesen létrejött a helyes (egy hónappal későbbi) határidővel. Minden teszt-adat törölve a végén.
  **Következő lépés:** Rob visszatértekor kipróbálhatja az új kontakt-mezőket/címkéket/ismétlődő teendőket; a `docs/minicrm-inspiracio.md`-ben felsorolt, még nem megépített elemek (automatizáció-szerkesztő UI, e-mail-kampányok, webes űrlap+Facebook lead-integráció, fájlmező egyedi mezőtípusként, Google Naptár-szinkron) továbbra is a 8. szekció backlogjában várnak döntésre.

  **Tizenhatodik forduló — szabályalapú Insights panel a Dashboardon (önálló folytatás, Rob távollétében):** a 8. szekció "Szabályalapú insights" backlog-tételének (5. pont) első, működő megvalósítása — Rob saját példáját véve alapul ("kevés a leadem mostanában → lead-generálásra fókuszálj"). **Elkészült:** `App\Services\InsightsEngine` — NEM AI-alapú, egyszerű küszöbérték-szabályokkal dolgozó osztály, ami négyféle javaslatot generálhat: (1) kevés új lead az elmúlt 14 napban → lead-generálásra fókuszálás javaslata; (2) legalább 14 napja ugyanabban a pipeline-szakaszban veszteglő nyitott üzletek száma → felkeresésre buzdítás; (3) lejárt határidejű, nyitott teendők száma; (4) ha még egyáltalán nincs se lead, se üzlet a fiókban → induló, "kezdd az első felvitellel" jellegű tipp. A Dashboardon egy új "Javaslatok" kártya jeleníti meg ezeket, státusz szerint színkódolva (piros/sárga/kék bal szegély, a már meglévő `text-danger`/`text-warning`/`text-info` tokenekkel — összhangban a WCAG-kontraszt-elvvel, nincs új szín bevezetve). **Tesztelve:** `tinker`-rel közvetlenül lefuttatva (üres fiókon helyesen mind a 4 típusú javaslat megjelent), majd a teljes Dashboard-nézet ténylegesen renderelve (Blade-hiba nélkül, 18 KB-os kimenet).

  Ugyanebben a fordulóban, folytatásként: **Pipeline tölcsér-diagram (Chart.js)** — a `riportok-terv.md`-ben már jelzett, eddig csak tervezett vizuális-megjelenítés ötlet első megvalósítása. A Chart.js könyvtár telepítve (`npm install chart.js`, csak a ténylegesen használt bar/tooltip/scale modulok regisztrálva, a teljes csomag helyett — kisebb bundle-méret). A Pipeline nézet (`deals/index.blade.php`) fölé egy vízszintes oszlopdiagram került, lépésenkénti nyitott üzletszámmal (funnel-hatás natív "funnel" chart-típus/plugin nélkül). A diagram színei futásidőben a jelenlegi CSS egyéni tulajdonságokból (`--accent-primary`, `--text-secondary`, `--border-subtle`) olvasódnak ki, így automatikusan illeszkedik bármelyik paletta/dark-light kombinációhoz, nincs hardkódolt szín. **Tesztelve:** `npm run build` hiba nélkül lefutott; a Pipeline nézet ideiglenes teszt-üzlettel renderelve (tinker), a `<canvas>` és a `renderFunnelChart` hívás ténylegesen megjelent a kimenetben; a teszt-üzlet utána `forceDelete()`-tel véglegesen törölve (a `Deal` modell soft-delete-et használ, sima `delete()` csak "papírkosárba" tenné).

  **Ugyanebben a fordulóban talált és javított valódi (nem csak teszt-) hiba:** a `php artisan test` végigfuttatásakor kiderült, hogy a **nyilvános regisztrációs űrlap ténylegesen el volt törve** — a `RegisteredUserController::store()` sosem lett frissítve a többfiókos (multi-tenant) architektúra bevezetésekor, így `User::create()` mindig `account_id`-t nem tartalmazó rekordot próbált beszúrni, ami a NOT NULL adatbázis-megkötés miatt éles hibát (SQLSTATE 500) dobott volna bárkinek, aki ma megpróbál regisztrálni. **Javítva:** regisztráláskor most tranzakción belül létrejön egy vadonatúj `Account` (a regisztráló nevével, egyedi slug-gal), a user ehhez kötve `role: owner`-ként, majd az account `owner_user_id`-ja is beállítva — ez a szokásos self-service SaaS-regisztrációs minta (meglévő fiókhoz csatlakozás/meghívás egy külön, később épülő invite-folyamat lesz, nem ez az űrlap). Emellett hiányzott egy `AccountFactory` (a `User::factory()` így soha nem tudott érvényes `account_id`-t generálni teszteléskor) — pótolva. Két elavult, a jelenlegi (soft-delete + owner/admin-only fiók-törlés) szabályokat még nem ismerő teszt is frissítve lett a tényleges, szándékos viselkedésre. **Eredmény:** a teljes automatizált teszt-csomag (25 teszt, korábban csak 4 futott le hibátlanul) most 100%-ban zöld.

  **Folytatás, még ugyanabban a fordulóban:** további három, önállóan megépített és tesztelt tétel a 8. szekció backlogjából — **adatminőség-jelzés** (hiányzó telefonszámú kontaktok az Insights panelben), **globális gyorskeresés** (`/search`, kontaktok/cégek/leadek/üzletek/projektek/retainerek között, nav-sávba épített keresőmezővel), és **személyes jegyzettömb** ("Jegyzeteim" — semmilyen rekordhoz nem kötött saját jegyzetek, teljes CRUD-dal; ehhez a `notes` tábla `noteable_type`/`noteable_id` mezői migrációval nullázhatóvá váltak). Mindegyik commitolva, pusholva, `php artisan test` mindvégig 25/25 zöld maradt. Részletek a 8. szekció megfelelő, ✅-jelölt bullet-jeiben.

  **Lezáró kutatási kör, még mindig a tizenhatodik fordulóban (Rob kérése: "a mini crm-ből minden hasznos ötletet vettünk-e át, ha nem keress; nézz utána a ConnectSoftnak is"):** a MiniCRM-dokumentáció újbóli, alaposabb átnézése két új ötletet hozott (teendő-sablon, ajánlat-sablon+egyszerű elektronikus aláírás — lásd `docs/minicrm-inspiracio.md` 9b. szakasz). A ConnectSoft-kutatás során kiderült, hogy az nem önálló szoftver, hanem a **GoHighLevel** nevű amerikai all-in-one platform magyar white-label viszonteladása — új, önálló dokumentum készült ([`docs/connectsoft-inspiracio.md`](docs/connectsoft-inspiracio.md)), ami világosan elkülöníti a ténylegesen CRM-releváns ötleteket (egységes kommunikációs idővonal, account-szintű "Snapshot"-sablon, automatikus review-kérés) azoktól a funkcióktól, amik túlmutatnak egy CRM hatókörén (website/funnel-builder, tagságplatform, affiliate, social media ütemezés) és Rob explicit döntésére várnak. Mindez bekerült a 8. szekció backlogjába dátumozva.

  **A tizenhatodik forduló összegzése, munkamenet-zárás (Rob kérése: "ha kell dokumentumot frissíteni tedd meg, ha nem innen folytatjuk később"):** ellenőriztem és frissítettem az 5. szekció (Fázisterv) táblázatát — a 3. fázis (MVP, csak Robnak) a fenti munka nyomán **lényegében készen áll** (a teljes Lead→Kontakt→Pipeline→Projekt/Retainer→Teendő/Jegyzet-lánc böngészőben működik, élesben tesztelt tenant-elkülönítéssel), a 4. fázis (Rob saját tesztelése) pedig innentől elkezdhető. **A projekt jelenlegi, ténylegesen nyitott pontjai:** (1) `docs/pipeline-sablonok.md` — a pipeline-lépések Rob tényleges munkafolyamatának validálására várnak (7. szekció); (2) a 8. szekció Ötlet-backlogja — nagyrészt Rob döntésére váró tételek, néhány (insights, gyorskeresés, saját jegyzetek, tagek, ismétlődő teendők, tölcsér-diagram) már megépítve; (3) semmilyen kritikus/blokkoló hiba vagy félbehagyott munka nincs — minden commit tesztelt és pusholt állapotban van. **Következő lépés, amikor Rob folytatja:** vagy kipróbálja a böngészőben az újdonságokat és visszajelez, vagy jelzi, melyik backlog-tétellel haladjunk tovább — addig is az AI önállóan folytathatja a még biztonságosan, önállóan eldönthető, jól körülhatárolt backlog-tételek megépítését (lásd 10. szekció, "Önálló munkavégzés" elv).

- **2026-07-26 (tizenhetedik forduló) — Lead-adatlap bővítése konkrét projekt-részletekkel (Rob kérése):** Rob kérte, hogy a leadeknél legyen: milyen jellegű a munka/projekt, mennyire tűnik nyerhetőnek (%), hol tart most a projekt, és mindig legyen egy (nem kötelező) kitölthető "következő lépés" + egy általános megjegyzés mező is — mindez látszódjon az összesítő kártyán is.
  **Elkészült:** migráció a `leads` táblához: a korábbi, általános célú `score` mező átnevezve **`win_probability`**-ra (pontosan tükrözi, hogy ez a "mennyire érzed nyerhetőnek %" válasz, nem egy elvont pontszám), plusz négy új mező: `project_title` (konkrét projekt/feladat megnevezése, a `service_type_id` kategóriájánál pontosabb), `current_status_note` (szabad szöveg: hol tart most), `next_step` (mindig kitölthető, de a validációban NEM kötelező), `next_step_due_at` (opcionális dátum a következő lépéshez). A már meglévő `notes` mező (eddig is létezett, csak "Jegyzet" címkével) most "Megjegyzés (egyéb infók)" néven egyértelműsítve. Mind a felvételi (`leads/create.blade.php`), mind a szerkesztő (`leads/edit.blade.php`) űrlap bővült ezekkel. **Az összesítő kártyán** (`leads/index.blade.php`) mostantól látszik: projekt megnevezése (+ szolgáltatás-típus), jelenlegi állapot-jelvény, "hol tart" szöveg (rövidítve), és a következő lépés + várható dátum, kiemelt (info) színnel. **Az Insights panel is bővült** egy új szabállyal: jelzi, ha nyitott leadeknél nincs megadva következő lépés — ez finoman rákényszerít arra, amit Rob kért ("mindig legyen kitöltve, de nem kötelezően").
  **Tesztelve:** tinker-rel végigfuttatva a teljes store→edit→index-render láncot minden új mezővel (mind helyesen mentve/megjelenítve a kártyán és a szerkesztő űrlapon is); `php artisan test` 25/25 zöld maradt. **Saját hiba, amit menet közben találtam és javítottam:** az első tesztfutás közben egy segéd-tesztszkript hibás `$errors` view-változó miatt kivétellel leállt, mielőtt a teszt-lead törlésre került volna — ez véletlenül bent hagyott egy `IDEIGLENES TESZT LEAD` rekordot az adatbázisban; ezt észrevettem (az Insights panel új szabálya jelezte ki), és `forceDelete()`-tel eltávolítottam, az adatbázis ismét tiszta.

  **Folytatás, még ugyanabban a fordulóban — architekturális megbeszélés + Deal leírás-mező (Rob kérése):** Rob rákérdezett, hogy a rendszer később bővíthető-e a ConnectSoft (ténylegesen GoHighLevel) irányába — a válasz: igen, az account-alapú multi-tenant felépítés, az esemény-hook rendszer és az előkészített `integrations`/`api_keys` táblák pont erre készültek, a funnel/landing-oldal builder az egyetlen rész, ami valójában más termékkategória (inkább integrációs pont, mint beépítendő funkció). Emellett Rob megkérdezte, hogy a "projekt scope" a lead szintjén kell-e vagy később — közös döntés: a Lead szinten a rövid `project_title` elég (a lead még minősítetlen), a részletes leírás a Deal szintjén hiányzott eddig teljesen.
  **Elkészült:** migráció — új `description` mező (text, nullable) a `deals` táblán, a `Project`/`Retainer` modellek mintájára. A Deal felvételi és szerkesztő űrlapja bővült ("Leírás — mit ajánlunk/tárgyalunk"). **Automatikus scope-öröklés:** amikor egy deal "won" állapotba kerül és emiatt automatikusan Project vagy Retainer jön létre (`DealController::maybeCreateProjectOrRetainer`), a leírás átöröklődik a gyerek-rekordba is — így a scope fokozatosan mélyül: Lead (`project_title`, röviden) → Deal (`description`, mit ajánlunk/tárgyalunk) → Project/Retainer (`description`, mit valósítunk meg ténylegesen). Menet közben egy másik, kisebb hiányosságot is találtam és javítottam: a `retainers/show.blade.php` eddig egyáltalán nem jelenítette meg a `description` mezőt, pedig a modellen már régóta létezik és a `projects/show.blade.php` régóta mutatja — most már a retainer-oldal is mutatja.
  **Tesztelve:** tinker-rel: deal létrehozása leírással → szerkesztő űrlapon helyesen megjelenik → "won" lépésre mozgatva → az automatikusan létrejött Project ténylegesen megkapta ugyanazt a leírást. `php artisan test` 25/25 zöld maradt.

  **Folytatás, még ugyanabban a fordulóban (Rob visszajelzése: a leírás fusson végig a teljes életúton):** eddig csak a Deal→Project/Retainer átmenetnél volt automatikus öröklés, a Lead→Deal átmenetnél nem — pótolva: `LeadController::convert()` mostantól a lead `project_title`, `current_status_note` és `notes` mezőit összefűzve átadja az új Deal `description` mezőjének, így a scope tényleg végigfut: Lead (röviden) → Deal (mit ajánlunk) → Project/Retainer (mit valósítunk meg), és minden lépésnél önállóan tovább is bővíthető (mind a négy modell `description`/hasonló mezője a saját szerkesztő űrlapján keresztül szabadon szerkeszthető, nem csak egyszeri másolat). Tesztelve tinker-rel (teljes konvertálási lánc, helyes szöveg-összefűzéssel), `php artisan test` 25/25 zöld maradt.

  **Kontakt-branding (Rob kérése, explicit "későbbi terv"):** Rob felvetette, hogy a kontaktoknál/cégeknél a jövőben legyen cégnév/brand, logó és fotó feltöltési lehetőség is — ez rögzítve a 8. szekció backlogjában, NEM épült meg (Rob explicit kérése, hogy egyelőre ne).

  **Architekturális megbeszélés + backlog-átrendezés (Rob kérdése: bővíthető-e a rendszer a GoHighLevel/ConnectSoft-irányba, és van-e hasonló a MiniCRM-nél is):** megerősítettem, hogy a jelenlegi account-alapú multi-tenant felépítés + esemény-hook rendszer + előkészített `integrations`/`api_keys` táblák pont erre az irányra készültek, nem kell újratervezni semmit. Rob kérésére a `docs/connectsoft-inspiracio.md` átstrukturálva: a korábban "nem javasolt" GoHighLevel-funkciók (tagság/kurzus, affiliate, közösségimédia-ütemezés, beépített fizetés) mostantól a rendszer **TÁVOLI (hosszú távú) scope-jában** szerepelnek, explicit GoHighLevel/ConnectSoft-eredettel megjelölve — **egyetlen továbbra is kizárt terület a website/funnel-builder marad** (Rob saját döntése, mert van saját webdesign-eszköze). Célzott utánakeresés a MiniCRM-nél is: van egy valódi, GoHighLevel-jellegű **Időpontfoglaló** funkciójuk (Google Naptár-szinkronnal, automatikus teendő-létrehozással) — ez megerősíti a már felvetett "nyilvános időpontfoglaló link" ötletet két független forrásból. A MiniCRM-nél NINCS landing-oldal-builder, LMS, közösségimédia-ütemezés, és a "affiliate"-nek tűnő funkciójuk valójában a MiniCRM saját ügyfélszerzési programja, nem az ügyfeleknek szánt eszköz — ez a fontos különbség dokumentálva (`docs/minicrm-inspiracio.md` 9c. szakasz). Részletek: [`docs/connectsoft-inspiracio.md`](docs/connectsoft-inspiracio.md) 3-4. szakasz, [`docs/minicrm-inspiracio.md`](docs/minicrm-inspiracio.md) 9c. szakasz.

  **Leírás-lánc fázis + időpont jelöléssel (Rob kérése):** Rob kérte, hogy a leírás-láncnál (Lead → Deal → Project/Retainer) látszódjon, melyik szövegrész melyik fázisban került hozzá, pontos dátummal/idővel. **Elkészült:** új `App\Support\DescriptionChain` segédosztály, ami a fázisváltásoknál (`LeadController::convert()`, `DealController::maybeCreateProjectOrRetainer()`) egy `[Fázis — ÉÉÉÉ.HH.NN. ÓÓ:PP]` fejlécet fűz a leírás elé/mögé, ahelyett hogy simán felülírná/összefűzné jelöletlenül. Fontos, tudatos döntés: **csak a fázisok KÖZÖTTI automatikus átvételnél** kerül be ilyen jelölés — egy fázison belüli szabad szerkesztés (pl. a Deal leírásának kézi bővítése a szerkesztő űrlapon) továbbra is sima szöveg marad, nem generál új bejegyzést minden mentésnél (ez szándékosan nem egy teljes verziótörténet/audit-log, csak a lánc mentén történő átadásokat jelöli — Rob kifejezetten "a leírás LÁNCNÁL" fogalmazott).
  **Menet közben talált és javított valódi hiba:** a tesztelés közben kiderült, hogy a `Lead` modellen a `notes` szöveges oszlop NEVE ÜTKÖZÖTT a már meglévő `notes()` polimorf relációval (mindkettő "notes"). Ez egy csendes, nehezen észrevehető hibát okozott: ha egy `Lead`-példány nem volt teljesen "hidratált" (pl. közvetlenül `Lead::create()` után, mielőtt egy friss adatbázis-lekérdezés visszatöltötte volna az összes oszlopot), a `$lead->notes` elérés a RELÁCIÓT (egy üres Collection-t) adta vissza a beírt szöveg helyett — ez pont a leírás-lánc tesztelésekor bukkant fel (`[]` jelent meg a szövegben szöveg helyett). **Javítva:** a `leads.notes` oszlop átnevezve `leads.comment`-re (migráció, modell, kontroller, mindkét űrlap, `schema.sql`/`adatmodell.md` frissítve) — így nincs több névütközés. **Tesztelve:** tinker-rel a teljes Lead→Deal→Project lánc, a leírásban ténylegesen mindkét fázis-fejléc (`[Lead — ...]`, `[Projektté vált — ...]`) helyesen, tiszta szöveggel megjelenik. `php artisan test` 25/25 zöld maradt.

- **2026-07-26 (tizennyolcadik forduló) — Ügyfélszerzés B) ág megépítve: "ki ajánlotta?" mező + kampány-nyilvántartás (Rob kérése: "csinálhatjuk a döntést igénylő feladatokat is, ami logikailag jól jönne"):** a `docs/ugyfelszerzes-terv.md`-ben már dokumentált, de eddig csak leírt Salesforce-minták (3.1/3.2. pont) tényleges megvalósítása — az A) ág (CRM saját ajánlói/partnerprogramja) változatlanul később esedékes, csak SaaS-fázisban aktuális.
  **Elkészült:** (1) `contacts.referred_by_contact_id` (self-referencing FK) — a kontakt felvételi/szerkesztő űrlapon választható, melyik meglévő kontakt ajánlotta; a kontakt-részletek oldal mindkét irányt mutatja ("ki ajánlotta" + "ő kiket ajánlott"), önhivatkozás validációval kizárva. (2) Új `campaigns` tábla + `App\Models\Campaign` + `CampaignController` (teljes CRUD) + önálló "Kampányok" menüpont — a `leads`/`deals` táblákon `campaign_id` FK a szabad szöveges `source` mező MELLETT (nem helyette). A kampány-részletek oldal riport-jelleggel mutatja a hozzá tartozó leadek számát, a nyitott/nyert üzletek számát és a nyert bevételt (`withCount`/`withSum` az indexnézetben is). A Lead→Deal konvertáláskor (`LeadController::convert`) a `campaign_id` is átöröklődik, konzisztensen a már meglévő leírás-lánc scope-öröklés elvvel. A globális gyorskeresésbe (`/search`) is bekerültek a kampányok.
  **Tesztelve:** új `tests/Feature/CampaignTest.php` (6 teszt: kampány létrehozás + account-hoz kötés, account-elkülönítés éles próbával — másik fiókos user 404-et kap show/edit/update/delete-nél is —, referral-lánc működése, önhivatkozás elleni validáció, kampány- és kontakt/lead/deal-űrlapok tényleges HTTP-renderelése). Emellett tinker-rel is végigfuttatva a teljes lánc (kampány → lead → won deal → riport-összegzés, referral mindkét irányban), minden teszt-adat törölve a végén. Teljes csomag: **31/31 zöld** (25 régi + 6 új).
  **Dokumentáció frissítve:** `docs/adatmodell.md`, `docs/schema.sql` (új tábla + FK-k), `docs/ugyfelszerzes-terv.md` és a 8. szekció (`docs/otlet-backlog.md`) ✅-jelölve.
  **Következő lépés:** a `docs/otlet-backlog.md` további, döntést igénylő tételei közül (pl. admin-szintű paletta-/mezőnév-testreszabás, riport-builder, automatizáció-szerkesztő UI) bármelyikkel folytatható, vagy Rob validálhatja a még nyitott `pipeline-sablonok.md`-t (7. szekció).

- **2026-07-26 (tizenkilencedik forduló) — Mentett szűrők/nézetek (Rob kérése: "mehetünk tovább sorba"):** a korábban felajánlott, még nem választott backlog-tétel megépítése (a másik kettő — duplikátum-felismerés, aktivitás-idővonal — továbbra is a listában vár).
  **Elkészült:** új `saved_filters` tábla (`account_id`, `user_id`, `resource`, `name`, `query_string`) + `App\Models\SavedFilter` + `SavedFilterController` (store/destroy) + újrafelhasználható `<x-saved-filters resource="..." index-route="...">` Blade-komponens, bekötve a Kontaktok és Leadek listaoldalba. A komponens a jelenlegi URL lekérdezés-részét ("`getQueryString()`") menti el névvel (pl. "Forró leadjeim" → `status=qualified`), és egy kattintással újra alkalmazható linkként jeleníti meg. Csak a szerzőjéhez tartozik — más felhasználó, még ugyanabban a fiókban is, nem törölheti (a `PersonalNoteController` mintájára, `AccessDeniedHttpException`-nel).
  **Tesztelve:** új `tests/Feature/SavedFilterTest.php` (5 teszt: létrehozás + account/user-hez kötés, más felhasználó nem törölheti ugyanabban a fiókban [403], másik account nem törölheti [404], érvénytelen `resource` elutasítva, a két listaoldal ténylegesen renderel a komponenssel). Tinker-rel is ellenőrizve a modell/kontroller viselkedése. Teljes csomag: **36/36 zöld** (31 régi + 5 új).
  **Dokumentáció frissítve:** `docs/adatmodell.md`, `docs/schema.sql`, `docs/otlet-backlog.md` ✅-jelölve, `crm_projekt.md` állapot-pointer.
  **Következő lépés:** a korábban felajánlott lista maradék két tétele (duplikátum-kontakt/lead felismerés e-mail/telefon alapján, aktivitás-idővonal a meglévő `activity_log`-ra építve) logikus folytatás, vagy Rob validálhatja a `pipeline-sablonok.md`-t.

- **2026-07-26 (huszadik forduló) — Duplikátum-kontakt/lead felismerés (Rob kérése: "mehetünk tovább sorba"):** a korábban felajánlott lista utolsó előtti tétele (az utolsó, aktivitás-idővonal, továbbra is a listában vár).
  **Elkészült:** új `App\Support\DuplicateFinder` segédosztály — **nem blokkoló** figyelmeztetés kontakt/lead felvételkor, e-mail (pontos egyezés, kis/nagybetűtől függetlenül) VAGY telefonszám (formázástól független egyezés — a tárolt és a beírt számból is levágjuk a szóközt/kötőjelet/zárójelet/`+`-t, és az utolsó 9 számjegyet hasonlítjuk, `SUBSTR(..., -9)` — így "+36 30 123 4567", "06301234567", "06-30-123-4567" mind ugyanaz a szám). Kontakt felvételekor a meglévő kontaktok között keres; lead felvételekor MIND a meglévő leadek, MIND a már kontakttá vált emberek között (két külön session-flash: `duplicate_leads` és `duplicate_contacts`), hogy ne vegyünk fel kétszer ugyanazt az érdeklődőt akkor sem, ha közben már ügyféllé vált. A találatok egy sárga figyelmeztető dobozban jelennek meg a kontakt-részletek / lead-szerkesztő oldalon, kattintható linkkel — **a mentést sosem akadályozzák meg** (lehet, hogy két különböző ember tényleg ugyanazt a céges telefonszámot használja).
  **Menet közben talált és javított hiba (tesztelés közben, még commit előtt):** az első verzió MySQL-specifikus `RIGHT()` SQL-függvényt használt, ami a teszt-adatbázisként használt SQLite-on nem létezik — a teszt nem hibázott, csak csendesen nem talált egyezést (`assertTrue` hamisra futott). Javítva `SUBSTR(..., -9)`-re, ami MySQL-en és SQLite-on is azonosan működik; utólag mindkét adatbázison (a valós MySQL fejlesztői DB-n tinker-rel, és a teszt-csomagban SQLite-on) leellenőrizve.
  **Tesztelve:** új `tests/Feature/DuplicateDetectionTest.php` (5 teszt: e-mail-egyezés jelzése mentés-blokkolás nélkül, eltérő formázású de egyező telefonszám felismerése, nem egyező adatoknál nincs jelzés, account-elkülönítés — másik fiók duplikátuma sosem szivárog át —, lead felvételekor mind lead-, mind kontakt-duplikátum jelzése). Teljes csomag: **41/41 zöld** (36 régi + 5 új).
  **Dokumentáció frissítve:** `docs/otlet-backlog.md` ✅-jelölve, `crm_projekt.md` állapot-pointer.
  **Következő lépés:** a lista utolsó tétele (aktivitás-idővonal a meglévő `activity_log`-ra építve) logikus folytatás, vagy Rob validálhatja a `pipeline-sablonok.md`-t.

- **2026-07-26 (huszonegyedik forduló) — Aktivitás-idővonal (Rob kérése: "mehetünk tovább sorba"):** a korábban felajánlott lista utolsó tétele — ezzel mindhárom eredetileg felajánlott backlog-tétel (mentett szűrők, duplikátum-felismerés, aktivitás-idővonal) elkészült.
  **Fontos felfedezés munka közben:** a `spatie/laravel-activitylog` csomag már a projekt legelejétől (2026-07-25) telepítve volt, az `activity_log` tábla is migrálva volt — de egyetlen modellen sem volt ténylegesen bekötve a `LogsActivity` trait, így a tábla mindig 0 sorral üresen állt (leellenőrizve: `Schema::hasTable('activity_log')` igaz, de `DB::table('activity_log')->count()` 0 volt). A korábbi haladási naplóbeli és backlog-beli "az activity_log már működik a háttérben" megfogalmazás emiatt pontatlan volt — most javítva mindkét helyen.
  **Elkészült:** új `App\Models\Concerns\HasActivityTimeline` trait (a csomag `LogsActivity` traitjére építve, `LogOptions::defaults()->logFillable()->logOnlyDirty()->dontSubmitEmptyLogs()` beállítással — csak a ténylegesen megváltozott, kitölthető mezőket naplózza), bekötve a `Contact`, `Deal`, `Lead`, `Project`, `Retainer` modellekre (Rob saját példája "kontaktnál/dealnél" alapján, kiegészítve a már meglévő Teendők/Jegyzetek-mintát követő Project/Retainer/Lead oldalakkal is a konzisztencia kedvéért). Új, újrafelhasználható `<x-activity-timeline :subject="...">` Blade-komponens: időrendben mutatja, ki (a bejelentkezett felhasználó, automatikusan "causer"-ként rögzítve) mikor mit csinált (létrehozta/módosította/törölte), módosításnál a ténylegesen megváltozott mezők nevével. Bekötve: kontakt-/projekt-/retainer-részletek oldal (a Teendők/Jegyzetek kártyák mellé), valamint a deal-/lead-szerkesztő oldal alja (ezeknek eddig nem volt önálló "show" nézetük).
  **Tesztelve:** új `tests/Feature/ActivityTimelineTest.php` (3 teszt: kontakt létrehozás+módosítás naplózása a helyes causer-rel és megváltozott mezőkkel, deal-aktivitás naplózása + a szerkesztő oldal ténylegesen megjeleníti, kontakt/projekt/retainer oldalak mindegyike hiba nélkül renderel a komponenssel). Valós MySQL fejlesztői adatbázison tinker-rel is végigfuttatva (kontakt létrehozás + két módosítás → helyes napló-sorrend, causer, megváltozott mezők), majd törölve. Teljes csomag: **44/44 zöld** (41 régi + 3 új).
  **Dokumentáció frissítve:** `docs/otlet-backlog.md` (✅-jelölés + a pontatlan "már működik" állítás javítása két helyen), `crm_projekt.md` állapot-pointer.
  **Összegzés (a Rob által korábban felajánlott 3 tételes lista lezárva):** mentett szűrők, duplikátum-felismerés, aktivitás-idővonal mind megépült és tesztelve van. **Következő lépés:** Rob validálhatja a `pipeline-sablonok.md`-t (7. szekció), vagy a `docs/otlet-backlog.md` további, döntést igénylő tételeivel (pl. admin-szintű paletta-/mezőnév-testreszabás, riport-builder, automatizáció-szerkesztő UI) folytatható tovább.

- **2026-07-26 (huszonkettedik forduló) — Kontakt-kártya átrendezése + Google Címtár-mintájú, szabadon elnevezhető elérhetőségek/mezők (Rob konkrét kérése):** Rob kérte, hogy a kontakt-kártyák cégnév / ügyfélnév-kontaktnév / elérhetőségek formában jelenjenek meg, hogy BÁRKI (nem csak admin) tudjon további e-mail címeket, telefonszámokat, címeket hozzáadni saját elnevezéssel (példa: egy "Bau-Haus" ügyfélnek 2 telefonszáma — "Mobil"/"Vezetékes" —, 2 címe — "Helyszín"/"Számlázási cím" —, és adószáma is legyen felvehető), plusz a végén teljesen szabad, egyedi mezők is bekerülhessenek (mint a Google Címtárban), és a keresés is terjedjen ki ezekre. Rob azt is jelezte, hogy egy külön "kapcsolat szintje" mezőre nincs szükség beépítve — ha valakinek kell, felveheti egyedi mezőként —, és hogy az el nem nevezett egyedi mezők "Egyedi mező 1", "Egyedi mező 2" stb. néven jelenjenek meg, amíg át nem nevezik őket.
  **Tervezési döntés:** a meglévő `contacts.email`/`phone`/`address` oszlopok (amikre már épül a duplikátum-felismerés és a kontakt-lista/keresés) maradnak elsődleges mezőként, ÚJ, kiegészítő `contact_fields` tábla tárolja a TOVÁBBI, tetszőlegesen elnevezhető bejegyzéseket — ez elkerüli a meglévő, már működő és tesztelt funkciók (duplikátum-detektálás, keresés) kockázatos átírását, miközben teljesíti a kért rugalmasságot.
  **Elkészült:** új `contact_fields` tábla (`account_id`, `contact_id`, `type`: email/phone/address/custom, `label` nullable, `value`, `sort_order`, cascade törlés a kontakttal együtt) + `App\Models\ContactField` + `Contact::contactFields()` reláció. Új `Contact::contactFieldsWithDisplayLabels()` segédmetódus: a `custom` típusú, el nem nevezett mezőket sorban "Egyedi mező 1", "Egyedi mező 2" néven számozza (ez CSAK megjelenítéskor számolt érték, nem tárolt adat), az email/phone/address típusú, el nem nevezett mezőknek pedig alapértelmezett típusnevet ad (E-mail/Telefon/Cím). Új, Alpine.js-alapú `<x-contact-fields-editor>` Blade-komponens — dinamikusan hozzáadható/eltávolítható sorok (típus-választó + elnevezés + érték mező), bekötve a kontakt felvételi és szerkesztő űrlapjába; a szándékosan üresen hagyott "+" sorok validáció előtt kiszűrésre kerülnek (`ContactController::dropEmptyContactFields`), hogy ne dobjanak hibát. A `ContactController::store()`/`update()` egy teljes csere-szinkronnal (`syncContactFields`) menti a beküldött listát. **Kártya-átrendezés:** a kontakt-lista kártyáin és a kontakt-részletek fejlécén mostantól a cégnév (ha van szervezet) jelenik meg elsőként, a kontakt neve alatta, majd egy külön "Elérhetőségek" blokk (elsődleges e-mail/telefon/weboldal/cím + az összes további `contact_fields` bejegyzés, a fenti címkézési logikával). **Keresés:** a helyi kontakt-keresés (`ContactController::index`) és a globális gyorskeresés (`SearchController`) egyaránt kiterjesztve a `contact_fields.value` mezőre (`whereHas`), account-szűrten.
  **Tesztelve:** új `tests/Feature/ContactFieldsTest.php` (7 teszt: több elnevezett mező felvétele egyben — pontosan a Bau-Haus-példa —, el nem nevezett egyedi mezők számozása, üres sorok csendes kiszűrése validációhiba helyett, szerkesztéskor teljes csere, helyi ÉS globális keresés talál plusz mezőre, account-elkülönítés — más fiók adata sosem szivárog át —, create/edit/show oldalak ténylegesen renderelnek a komponenssel). Valós MySQL fejlesztői adatbázison tinker-rel is végigfuttatva pontosan a Bau-Haus-forgatókönyvvel (2 telefon, 2 cím, adószám, 1 el nem nevezett mező) — helyes számozás és kereshetőség megerősítve, teszt-adat törölve. Teljes csomag: **51/51 zöld** (44 régi + 7 új).
  **Dokumentáció frissítve:** `docs/adatmodell.md`, `docs/schema.sql` (új tábla + FK), `crm_projekt.md` (4. szekció táblázat + állapot-pointer).
  **Következő lépés:** Rob kipróbálhatja böngészőben az új kártya-elrendezést és a "+ Elérhetőség/mező hozzáadása" funkciót; ha később az Organizationökhöz (cégekhez) is hasonló rugalmas mezőrendszer kellene, az egy önálló, ugyanerre a mintára épülő következő lépés lehet (jelenleg az Organization modellnek nincs is saját CRUD-felülete).

- **2026-07-26 (huszonharmadik forduló) — "+ Új létrehozása..." minta a lenyíló mezőknél, kritikai visszajelzéssel (Rob kérése + explicit felkérés a kritikus visszajelzésre):** Rob azt kérte, hogy minden "fix" lenyíló mezőnél (Kampány, Ki ajánlotta) legyen szabad szavas hozzáadási lehetőség is, mert pl. egy még nem létező kampány vagy egy még nem kontaktként szereplő ajánló esetén jelenleg nincs mit választani. Emellett explicit felkért, hogy a jövőben MINDIG legyek kritikus, ha egy kérése ütközik a bevált CRM-gyakorlattal, mert még sosem épített CRM-et.
  **Kritikai visszajelzés (Rob elfogadta, rám bízta a részleteket):** a "dropdown + külön szabad szöveges mező" pont azt a problémát hozná vissza, amit a strukturált Kampány-tábla és a Ki ajánlotta-kapcsolat épp meg akart oldani — két, egymással nem összefüggő adatforrás (pl. "Facebook nyár" szabad szövegként kétféle írásmóddal sosem kapcsolódna össze egy riportban). Helyette a nagy CRM-ek (Salesforce/HubSpot/Notion) bevált "+ Új létrehozása..." mintáját vezettem be: a lenyíló lista MAGA kínálja fel az új rekord létrehozását, ami egy kattintással VALÓDI, önálló rekordot hoz létre (nem szöveges duplikátumot).
  **Elkészült:** új `App\Support\SelectOrCreate` segédosztály (`firstOrCreate` névvel — ha véletlenül ugyanazt a nevet gépeli be valaki újra, nem jön létre felesleges másolat, és account-szűrten működik). Két új Blade-komponens: `<x-select-or-create>` (általános, egy "name" mezős gyors-létrehozás — Kampány és Szervezet mezőknél) és `<x-referrer-select>` (kontakt-specifikus, mert egy ajánló felvételéhez több mező is kell: keresztnév kötelező, vezetéknév/e-mail/telefon opcionális — és Rob saját fallback-ötletét valósítja meg: ha az ajánló még nincs a kontakt-listában, itt rögtön fel is vehető, mint valódi új kontakt, nem szöveges bejegyzésként). Bekötve: Kampány (lead/deal felvételi és szerkesztő űrlap), Szervezet és Ki ajánlotta (kontakt felvételi és szerkesztő űrlap).
  **Kiegészítő, önállóan felismert hiányosság + javítás (Rob kérése alapján, hogy mindig jelezzek ilyet):** mivel a Szervezet mezőnél mostantól bárki hozhat létre új céget, de az Organization modellnek eddig NEM volt semmilyen saját kezelőfelülete (csak a kontakt-űrlap lenyíló listájából volt elérhető), ez láthatatlan, kezelhetetlen rekordokat halmozott volna fel — pótolva egy minimális `OrganizationController` (lista/keresés, részletek a hozzá tartozó kontaktokkal, szerkesztés, törlés — létrehozás szándékosan NINCS önálló oldalon, az a gyors-létrehozáson keresztül történik), új "Szervezetek" menüpont.
  **Másik, önállóan felismert és javított hiba:** a duplikátum-kereső (`DuplicateFinder`, 2026-07-26 korábbi forduló) csak a kontaktok FŐ email/phone mezőjét nézte — miután a kontaktoknak mostantól tetszőleges számú TOVÁBBI elérhetősége is lehet (`contact_fields`), egy csak ott rögzített második telefonszám/e-mail láthatatlan maradt volna a duplikátum-jelzésnek. Kiterjesztve: Contact típusnál a `contactFields` táblát is átvizsgálja (email/phone típusú bejegyzések, ugyanazzal a formázás-független telefon-normalizálással).
  **Nem javasolt, Rob egyetértett:** a strukturális/konfigurációs lenyíló mezőknél (Szolgáltatás-típus, Pipeline, Pipeline-lépés, állapot-enumok) NEM vezettem be a gyors-létrehozást — egy új szolgáltatás-típushoz pl. pipeline is kell, különben törné a Lead→Deal automatikus konverziós láncot (`LeadController::convert()`). Rob elfogadta ezt a megkülönböztetést.
  **Tesztelve:** új `tests/Feature/SelectOrCreateTest.php` (8 teszt: kampány gyors-létrehozás lead/deal felvételkor, ugyanazzal a névvel nem duplikál, szervezet gyors-létrehozás, ajánló gyors-létrehozás valódi kontaktként, üresen hagyott ajánló-név csendben figyelmen kívül marad, Szervezetek lista/részletek oldal renderel, account-elkülönítés — más fiók azonos nevű kampánya nem kerül újrafelhasználásra). A `DuplicateDetectionTest`-hez egy új teszt a `contact_fields`-es telefonszám felismerésére. Valós MySQL fejlesztői adatbázison tinker-rel is végigfuttatva (kampány/szervezet gyors-létrehozás, névismétlésnél nincs duplikáció, meglévő kiválasztása változatlan id-t ad vissza). Teljes csomag: **60/60 zöld** (51 régi + 9 új).
  **Memória-bejegyzés (jövőbeli munkamenetekhez):** Rob explicit kérése alapján két új memória-fájl készült — az egyik rögzíti, hogy mindig kritikusan kell viszonyulni a kéréseihez CRM best practice szempontból (ő maga jelezte, hogy sosem épített még CRM-et), a másik egy futó lista az eddig észlelt, de még nem javított feszültségekről (pl. a Lead `next_step` szabad szöveges mezője nem kapcsolódik a tényleges Task-rendszerhez — ezt NEM javítottam most, csak jegyzeteltem, mert kívül esett a mai kérés körén — máskor érdemes visszatérni rá).
  **Következő lépés:** Rob kipróbálhatja a "+ Új létrehozása..." mezőket böngészőben; a `project_known_tensions.md`-ben jegyzett pontok (next_step/Task-rendszer, Organization-szintű rugalmas mezők) jövőbeli döntésre várnak.

- **2026-07-26 (huszonnegyedik forduló) — Önálló kritikai audit + javítások (Rob kérése: "van még olyan elem, ami szabotálja a profi eredményt? Vedd ki, legyen helyette best practice"):** teljes önálló átvizsgálás, majd megállás nélküli, folyamatos javítás Rob explicit felhatalmazására ("dolgozz folyamatosan... ha komoly kérdésed van, azt tedd fel most, egyébként később").
  **Egy felmerült gyanú, amit TESZTTEL cáfoltam (fontos, nehogy tévesen riasszak):** felmerült, hogy az `InsightsEngine`-ben lévő `Contact::whereNull('phone')->orWhere('phone', '')->count()` (becsomagolás nélküli top-level `orWhere` egy account-szűrt global scope mellett) SQL-precedencia miatt átszivároghatna másik fiók adatán. Valós MySQL-en, két külön teszt-fiókkal reprodukálva **NEM igazolódott** — a `toSql()` kimenet megmutatta, hogy Laravel `Builder::callScope()`-ja szándékosan, biztonságosan csoportosítja a global scope előtt hozzáadott feltételeket (`addNewWheresWithinGroup`), pont az ilyen hiba elkerülésére. Ez a rendszer beépített védelme, nem hiba — dokumentálva, hogy legközelebb ne kelljen újra átvizsgálni.
  **Ténylegesen talált és javított hiányosságok:**
  1. **Lead→Contact konvertáláskor a lead szabad szöveges `company` mezője eddig egyszerűen ELVESZETT** — sosem lett belőle Szervezet-kapcsolat a Contacton. Javítva: `LeadController::convert()` most a `company` alapján köti/hozza létre a Szervezetet (`SelectOrCreate::firstOrCreateByName`, kis/nagybetűtől függetlenül).
  2. **`deals.organization_id` a UI-ban SOHA nem volt kitöltve** (nem volt hozzá form-mező) — mindig NULL maradt, és ez öröklődött tovább a belőle létrejövő Project/Retainer rekordokba is. Javítva: `DealController::store()`/`update()` automatikusan átveszi a kiválasztott kontakt szervezetét (Salesforce Account-öröklés mintája).
  3. **GDPR hozzájárulás (`gdpr_consent_at`/`gdpr_consent_note`) mezőknek eddig NEM volt semmilyen UI-ja**, pedig a `crm_projekt.md` 3. szekció kifejezetten "kritikus szabályként" említi. Pótolva: checkbox + megjegyzés mező a kontakt felvételi/szerkesztő űrlapon (`docs/gdpr-terv.md` 1. pont szerint) — a hozzájárulás időpontja csak első bejelöléskor íródik, nem íródik újra minden mentésnél, kikapcsoláskor pedig nullázódik (visszavonás). A módosítás automatikusan bekerül az aktivitás-idővonalba is (a már meglévő `HasActivityTimeline` mechanizmuson keresztül, mivel a mező a Fillable listában van).
  4. **Kis/nagybetű-érzékeny `firstOrCreate`** a `SelectOrCreate`-ben és a `Tag::firstOrCreate`-ben — "Bau-Haus Kft." és "bau-haus kft." két külön rekordot hozott volna létre. Javítva: mindkét helyen kis/nagybetűtől független keresés fut a létrehozás előtt.
  5. **A duplikátum-jelzés eddig csak felvételkor futott, szerkesztésnél nem** — ha valaki egy meglévő kontakt/lead e-mailjét/telefonját egy MÁSIK meglévő rekordéra írta át, semmilyen figyelmeztetés nem jelent meg. Javítva: `ContactController::update()` és `LeadController::update()` is lefuttatja a `DuplicateFinder`-t.
  6. **Az Insights panel nem jelezte, ha egy lead "következő lépés" határideje lejárt** (csak azt jelezte, ha egyáltalán nincs megadva) — új szabály pótolva.
  **Tesztelve:** új `tests/Feature/BestPracticeFixesTest.php` (8 teszt: lead-cégnév → szervezet-kapcsolat, kis/nagybetűtől független újrafelhasználás, deal szervezet-öröklés, GDPR checkbox rögzítés + nem-újraírás + visszavonás, duplikátum-jelzés szerkesztéskor, lejárt next_step insight-szabály). Teljes csomag: **68/68 zöld** (60 régi + 8 új).
  **Memória-frissítés:** a `project_known_tensions.md` mostantól ide is mutat referenciaként (a lezárt tételek onnan nem törlődnek, csak a jövőbeli, még nyitva maradó pontok — Organization-szintű rugalmas mezők, Lead next_step/Task-rendszer viszonya, kontakt-duplikátum ÖSSZEVONÁS hiánya — kerülnek bele frissítve).
  **Következő lépés, még ugyanebben a fordulóban folytatva:** a legnagyobb, eddig 0%-ban megvalósított hiányosság — a `custom_field_definitions` mechanizmus (ez adná a rendszer "kódolás nélküli univerzalitását", crm_projekt.md kiemelt elvárása) — építése folyamatban, lásd alább.

  **Folytatás, még ugyanabban a fordulóban — az egyedi mezők rendszer tényleges megépítése:** a `custom_field_definitions` tábla és `App\Models\CustomFieldDefinition` modell a projekt legelejétől (2026-07-25) létezett, jól átgondolt oszlopokkal (entity_type, field_key, label, field_type, options, is_required, sort_order, service_type_id) — de egyetlen felület sem használta, a "kódolás nélküli univerzalitás" (crm_projekt.md 3. szekció kiemelt elvárása) csak papíron létezett.
  **Elkészült:** teljes admin-felület (`/custom-field-definitions` — lista entitás-típusonként csoportosítva, felvétel, szerkesztés, törlés), új `App\Support\CustomFieldFormHelper` (definíciók lekérdezése entitás-típus + opcionális szolgáltatás-típus szerint, dinamikus validációs szabályok generálása típusonként — szám/dátum/igen-nem/választólista/URL), és két új, újrafelhasználható Blade-komponens: `<x-custom-fields-form>` (dinamikusan renderel beviteli mezőt minden aktív definícióhoz — szöveg/hosszú szöveg/szám/dátum/checkbox/választólista/többszörös választólista/URL típusonként eltérő input-tal) és `<x-custom-fields-display>` (csak-olvasható megjelenítés). A `field_key`-t a rendszer generálja a felhasználó által beírt "label"-ből (kódolás nélküli szellemben — nem kell technikai kulcsot kitalálnia), ütközés esetén sorszámmal egyedivé téve. **Bekötve:** Kontakt (felvétel/szerkesztés/részletek) és Üzlet (felvétel/szerkesztés) — a Deal esetén a szolgáltatás-típus a kiválasztott pipeline-ból származik, így egy mező beállítható úgy, hogy csak egy adott szakmánál (pl. csak coachingnál) jelenjen meg, vagy mindenhol. Egy definíció törlése a már felvitt értékeket NEM törli a rekordokról, csak az űrlapról tünteti el (nem visszafordíthatatlan adatvesztés).
  **Tudatos, dokumentált egyszerűsítés:** a Deal felvételi űrlapján a pipeline lenyíló lista Alpine.js-es, kliens-oldali váltású — az egyedi mezők viszont szerver-oldalon, a KEZDETBEN kiválasztott pipeline szolgáltatás-típusa alapján renderelődnek. Ha valaki menet közben, oldal-újratöltés nélkül vált másik pipeline-ra, a mezők nem frissülnek élőben — ez egy tudatosan vállalt, dokumentált korlát (a gyakorlatban a "+ Új üzlet" gomb szinte mindig már a helyes pipeline-ra szűrve nyit), nem hiba.
  **Menet közben talált és javított valódi Blade-hiba:** az admin-lista nézetben két egymást követő `@if...@endif` direktíva közvetlenül egymás mellé kerülve (`@endif@if`, szóköz nélkül) lefordítási hibát okozott ("unexpected token endif") — ezt a saját automata tesztem fogta meg (a render-teszt 500-as hibát adott), NEM a fejlesztés közbeni manuális ellenőrzés. Javítva a két direktíva külön sorba tördelésével.
  **Tesztelve:** új `tests/Feature/CustomFieldDefinitionTest.php` (9 teszt: definíció létrehozása + field_key generálás, névütközés-kezelés sorszámmal, választólista vesszős bevitelből tömbbé alakítása, definiált mező ténylegesen megjelenik és mentődik a kontakt-űrlapon, kötelező mező üresen tiltja a mentést, szolgáltatás-típushoz kötött mező csak az adott típusú dealnél jelenik meg — más típusnál nem —, account-elkülönítés, admin-oldalak renderelése, definíció törlése nem törli a már mentett értékeket). Valós MySQL fejlesztői adatbázison tinker-rel is végigfuttatva. Teljes csomag: **77/77 zöld** (68 régi + 9 új).
  **Dokumentáció frissítve:** `docs/adatmodell.md` (a `custom_field_definitions` szekció + a 3. szekció "kódolás nélküli univerzalitás" bekezdése frissítve, hogy tükrözze a tényleges állapotot).
  **Összegzés — a Rob által kért kritikai audit ezzel lezárva:** mind a 7 talált/épített tétel elkészült (6 kisebb javítás + az egyedi mezők rendszer). **Következő lépés:** Rob böngészőben kipróbálhatja az egyedi mezőket (pl. "Felmérés pontszám" a coachingnál), és validálhatja a még nyitott `pipeline-sablonok.md`-t; jövőbeli bővítési lehetőség (nem sürgős): az `entity_type` kiterjesztése Organization/Project/Retainer-re, ha felmerül az igény.

  **Folytatás, még ugyanabban a fordulóban (Rob kérése: "dolgozz folyamatosan, egyik feladatról a másikra"):** a kritikai audit lezárása után két további, jól körülhatárolt, döntést nem igénylő backlog-tétel megépítése.
  1. **Gyors-felvétel mobilon** — lebegő "+" gomb minden bejelentkezett oldalon (`<x-quick-add-fab>`, `layouts/app.blade.php`), egy kattintással "+ Új lead"/"+ Új kontakt" opció. A "teendő" szándékosan kimaradt: a `tasks` tábla `taskable_type`/`taskable_id` mezői kötelezőek (nem nullázhatók, mint a `notes`-nál a "saját jegyzet" esetén), egy önálló, semmihez nem kötött gyors-teendő séma-módosítást igényelne — külön backlog-tételre hagyva, ha felmerül az igény.
  2. **Audit napló admin-felület** — új `/activity-log` oldal, entitás-típus szerint szűrhető, fiók-szintű, összesített aktivitás-lista (a már meglévő rekordonkénti `<x-activity-timeline>` mellé). Fontos technikai részlet: az `activity_log` táblának (spatie-csomag) nincs saját `account_id` oszlopa, ezért a tenant-elkülönítést kézzel kellett megoldani — csak az aktuális fiók FELHASZNÁLÓI (`causer_id` → `users.account_id`) által okozott bejegyzések látszanak, valós MySQL-en is leellenőrizve.
  **Tesztelve:** `tests/Feature/QuickAddFabTest.php` (2 teszt) + `tests/Feature/ActivityLogPageTest.php` (3 teszt — beleértve, hogy másik fiók aktivitása sosem szivárog át). Teljes csomag: **82/82 zöld** (77 régi + 5 új).
  **Dokumentáció frissítve:** `docs/otlet-backlog.md` mindkét tétel ✅-jelölve.
  **Következő lépés:** a `docs/otlet-backlog.md`-ben még maradt, nagyobb, Rob-döntést igénylő tételek (Automatizáció-szerkesztő, Sablonok kezelése, Riport-builder, Finomabb jogosultságok, Import/duplikátum-szabályok admin-állítása, Mezők sorrendje/láthatósága) várnak — vagy Rob validálhatja a `pipeline-sablonok.md`-t.

